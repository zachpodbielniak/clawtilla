#+title: Clients
#+description: The CLI, the GTK app, the web client, and embedding libclawt
#+authors: Zach Podbielniak
#+categories: software documentation
#+created: [2026-08-21 Fri]
#+updated: [2026-08-30 Sun]
#+version: 0.2.0

* Clients

Four front ends, one daemon.  None of them holds state or a transport of
its own, which is why two of them open at once agree with each other.

The two graphical ones -- ~clawtilla-gtk~ and ~clawtilla-web~ -- are held
to the same feature set by ~make parity~, so a capability added to one is
a build failure until it exists in the other.

** The CLI

#+begin_src bash
clawtilla [OPTION...]
clawtilla <command> [ARGS...]
#+end_src

Global options: ~-c/--config~, ~-s/--socket~, ~-p/--profile~,
~-H/--host~, ~--port~, ~--token~, ~-V/--version~, ~--license~,
~--generate-config~, ~--generate-systemd-service~.

They go *before* the verb.  Option parsing stops at the first word that
is a command, and everything from there is handed to the subcommand
untouched -- so ~clawtilla agent list -s /path~ does not set the socket.
That costs post-verb globals and buys back a message body: GOption
removes what it recognises wherever it appears, so
~clawtilla send bot read --license and summarize~ used to print the
licence, send nothing, and exit 0.

*** Commands

| Command | What it does |
|---------+--------------|
| ~daemon~ | run the daemon in the foreground (same code as ~clawtillad~) |
| ~daemon token~ | print the bearer token a remote client must present |
| ~remote list/add/rm~ | saved daemons on other machines |
| ~status~ | version, build, hold, update, agent and client counts |
| ~pause [agent]~ | hold the fleet: stop delivery, let running turns finish |
| ~resume [agent]~ | take the hold off; queued work goes out in order |
| ~agent list~ | id, state, queue depth, link, description |
| ~agent show <id>~ | everything about one agent |
| ~agent create --id X [--name…] [--model…] [--computer…] [--image…] [--vm-image PATH] [--no-start]~ | |
| ~agent new~ | answer six questions; a model drafts it |
| ~agent new --ai "<description>" [--commit]~ | the same, non-interactively |
| ~agent rm <id> [--with-computer] [--purge]~ | remove from the config; the flags say how much else goes |
| ~agent start/stop/restart <id>~ | |
| ~agent reset <id>~ | clear its AI session so it reads its identity files again |
| ~agent logs <id>~ | the child's recent output |
| ~agent set <id> <key> <value>~ | e.g. ~model.effort high~ |
| ~agent files <id>~ | the workspace's org files, and which reach the prompt |
| ~agent mount list/add/rm <id>~ | folders shared with its computer |
| ~agent edit <id> [FILE...]~ | open them in ~$EDITOR~ |
| ~agent discover~ | agents on disk that the config does not know about |
| ~agent import <id> [--from DIR \vert{} --link DIR \vert{} --git URL] [--keep-git]~ | adopt one, keeping its state |
| ~agent forget <id>~ | move one aside; nothing is deleted |
| ~agent git-init~ | make the state directory a git repository, with a ~.gitignore~ that keeps credentials out |
| ~send <target> <message…>~ | to an agent or a room |
| ~chat <agent>~ | interactive |
| ~mailbox list/dead/ack/requeue/purge <agent> [item]~ | |
| ~room list/create/add/history~ | |
| ~team list~ | teams, what each handles, who leads it, how many running |
| ~team create <id> [DESCRIPTION]~ | |
| ~team set <id> <key> <value>~ | ~name~, ~description~, ~color~, ~order~ |
| ~team rm <id>~ | its agents keep running and become teamless |
| ~team assign <agent> <team> [lead]~ | |
| ~task list/show/cancel~ | |
| ~decision list [--all]~ | choices agents are waiting on you for |
| ~decision answer <id> <answer>~ | free text; it routes back to the agent |
| ~decision dismiss <id>~ | it carries on with its default |
| ~event list [subject]~ | what the fleet has been doing, from the event log |
| ~cost [--today \vert{} --days N] [--agent ID]~ | what the fleet has spent |
| ~computer exec <agent> -- CMD…~ | |
| ~computer status <agent>~ | |
| ~cp <src> <dst>~ | either side may be ~<agent>:<path>~ |
| ~config show/validate/render/edit~ | |
| ~config reload~ | re-read the config file the daemon was started with |
| ~folders list/add/rm~ | folders every agent with a computer gets |
| ~memory list/search <agent>~ | what an agent has remembered; read-only |
| ~model~ | every provider and model, and which can run an agent |
| ~image~ | container images clawtilla suggests, and your own |
| ~image vm list/catalog/get/rm/cancel~ | disk images for VM agents |
| ~connector catalog/list/add/connect/key/refresh/revoke/rm~ | accounts clawtilla holds the credential for |
| ~plugin list~ | |
| ~integration types/list/show~ | what kinds exist, and what is configured |
| ~integration add/set/scope/rm~ | one bot, some bots or all of them |
| ~integration health <agent> [name]~ | |
| ~integration matrix-login <name> <homeserver> <user>~ | password from stdin |
| ~integration matrix-rooms <name>~ | |
| ~integration test <name>~ | send one notification now |
| ~routine list/add/set/rm/run~ | standing work on a schedule |
| ~computer rebuild <agent>~ | destroy the guest and build it again from the image |

Two verbs are not for people.  ~connector relay <name>~ and
~computer desktop-mcp <agent>~ speak MCP on stdout and are what an
agent's =.mcp.json= names; running either by hand gets you a protocol
stream.

Each subcommand owns its own option parsing rather than sharing the
global table.  One shared table across two dozen verbs collapses into
mutually exclusive flags that are each valid for one verb, and the
~--help~ output stops being readable.

*** Applying an identity change: ~agent reset~, not ~agent restart~

An AI CLI is handed a system prompt when a session *starts* and never
when it resumes one.  So editing =SOUL.org=, or repointing
~persona.identity_files~, reaches a running agent's files and not its
prompt -- and restarting it does not help either, because the next turn
resumes the session that already exists.  ~agent reset~ is the one that
applies it: it stops the agent, moves its session files aside, clears
its stored sessions from libreclaw's database, and starts it again if it
was running.  A stopped agent stays stopped.

~agent set~ says so on the way past.  A ~persona.*~ key reports
~restart_required~ like a ~tools.*~ key does, and the CLI gives
different advice for each, because the remedies differ: a tool list is
read at session start, so a restart is enough there.

Both places have to be cleared.  libreclaw restores a session from
~session.persist_dir~ *or* from its database, so clearing one leaves the
agent resuming from the other and looking like the reset did nothing.

*** ~agent create~ takes any ~--key value~, with the dashes folded

Each flag becomes a payload member with ~--~ stripped and every ~-~
turned into ~_~, so ~--vm-image~ arrives as ~vm_image~ and the daemon
decides what is valid.  Passing the dashed spelling through verbatim
produced a member the daemon has never heard of, which it ignores -- so
a VM agent could be given a disk from the command line, be told it had
been created, and have no disk.

~--no-start~ is the exception, because it carries no value of its own:
it sends ~start: false~.  Without it the daemon starts what it just
made, which is the point -- a computer is built at start, so an agent
created and left alone has a config file and no machine.

The same fold is in ~connector add~, where ~--client-id~ becomes
~client_id~.

*** Why the event log has a verb here

~ClawtEventLog~ has recorded every published event since the daemon was
written, and ~event.list~ exists so that diagnosing a message loop is a
control rather than ~sqlite3~ and ~grep~ on the host.

Both graphical clients read it. From a terminal it was unreachable ---
which is the one place somebody is standing when they have ssh'd in to
find out what a fleet did overnight, and exactly the case the feature
was built for. Found by running a real fleet and reaching for it.

Without a subject it is fleet-wide, because watching several agents at
once is the case that sends people to the shell.

*** Reaching a daemon on another machine

Every verb takes ~--host~, ~--port~ and ~--token~, or ~--profile~ to use
one saved earlier:

#+begin_src sh
# On the machine running the fleet
clawtilla daemon token

# On the machine you are sitting at
clawtilla remote add workstation 100.72.0.41 --token "$(ssh box clawtilla daemon token)"
clawtilla --profile workstation agent list
clawtilla --profile workstation send researcher "how far did you get?"
#+end_src

~--host~ beats ~--profile~, which beats the local socket.  The explicit
address is both last-resort and first-priority for the same reason: it is
what you reach for when a saved profile is the thing you are working
around.

~daemon token~ reads the token from disk rather than asking the daemon
for it, because nothing may write a secret's value into an IPC response.
That also means it only works on the machine the daemon runs on, which is
the point -- it is how you get the token to copy somewhere else.

~remote list~ never prints a token.  It is the command you run to check
what you saved, often over somebody's shoulder.

The profiles live in ~$XDG_CONFIG_HOME/clawtilla/connections.yaml~, mode
0600, and are shared with the GTK client's connection menu.  They are in
the *client's* config rather than in ~clawtilla.yaml~ on purpose: the
point of a profile is to reach a daemon that is somewhere else, and a
laptop connecting to a workstation may have no fleet, no state directory
and no ~clawtilla.yaml~ of its own.

A verb makes one request and exits, so the CLI has nothing to reconnect
for and does not try.  What the keepalive on a network connection
changes here is how a dead route *reports*: the connection is given up
after about a minute and the verb fails with "the daemon closed the
connection", instead of sitting out the request's 120-second deadline
and reporting a timeout.  The same outcome a minute earlier, and a truer
sentence -- a timeout reads as a slow daemon, and this one is not there.

*** Choosing where the daemon listens

~clawtillad~ takes ~--bind~ and ~--no-bind~, and its ~--help~ prints what
this machine would listen on with neither:

#+begin_example
Listening, with no --bind:

  unix     /run/user/1000/clawtilla/daemon.sock
  tailnet  100.72.0.41:8792  (daemon.tailscale: true)
  tcp      off (daemon.tcp_enabled: false)
#+end_example

The real address and the real port, not a description of them -- the
whole reason to reach for these flags is wanting to know, and change,
exactly that.

#+begin_src sh
clawtillad --no-bind                    # unix socket only
clawtillad --bind 127.0.0.1:8792        # somewhere specific instead
clawtillad --bind 10.0.0.5:9000 --bind '[fd7a:115c:a1e0::1]:9000'
#+end_src

~--bind~ *replaces* the network addresses rather than adding to them, so
a daemon told where to listen is not also listening somewhere it was not
asked to. It is repeatable, and each address carries its own port; the
port defaults to ~daemon.tcp_port~ when one is not given.

An address you name must bind. That is the difference between it and the
tailnet address, which clawtilla chose for you and only warns about: a
stale process holding the tailnet port should not stop the fleet, but a
daemon that ignored where you told it to listen and started anyway would
be running somewhere you did not know about.

Addresses are parsed before anything is created, so a typo is refused
while you are still looking at the command line:

#+begin_example
clawtillad: --bind: 'localhost' is not a numeric address; clawtillad binds
addresses rather than resolving names
#+end_example

Names are refused rather than resolved on purpose: resolution is a
network round trip on the path that starts the daemon, and a name can
resolve to an address on a different network than the one intended.

IPv6 with a port needs brackets -- ~[fd7a:115c:a1e0::1]:9000~ -- because
an address full of colons has no last-colon to split on. Without a port,
a bare ~fd7a:115c:a1e0::1~ is fine.

*** Exit statuses that mean something

- ~computer exec~ exits with the *command's* exit status, so
  ~clawtilla computer exec … && next-thing~ behaves the way a shell user
  expects
- ~integration health~ exits non-zero if any check failed, so it is
  usable in a pre-flight script
- a subcommand with no arguments prints usage and exits non-zero

*** Connecting an account

#+begin_src bash
clawtilla connector catalog
clawtilla connector add gh --provider github --account work \
    --command github-mcp-server --scope all
clawtilla connector key gh          # reads stdin with the echo off
clawtilla connector list
#+end_src

There is deliberately no =--key= flag, for the same reason there is no
=--password= on the Matrix login: an argument is in your shell history
and in the process table.

=clawtilla connector relay <name>= is the verb an agent's =.mcp.json=
names.  It speaks MCP on stdout and is not for people.

See [[file:connectors.org][connectors.org]].

*** Signing in to Matrix

#+begin_src bash
clawtilla integration add home matrix
clawtilla integration matrix-login home matrix.example.org researcher
#+end_src

The password is read from stdin, and from a terminal without echoing.
There is deliberately no ~--password~ flag: an argument is in the shell
history and in the process table for as long as the command runs, which
is long enough.

Piping works too, for a script:

#+begin_src bash
pass show matrix/researcher | clawtilla integration matrix-login \
    home matrix.example.org researcher
#+end_src

Nothing secret comes back.  The token is written to a file on the
daemon's disk and the config is given its path; the reply says which
file, and the user id the server assigned.  See
[[file:integrations.org][integrations.org]].

** The GTK client

#+begin_src bash
clawtilla-gtk [--socket PATH]
#+end_src

GTK4 and libadwaita.  Agents are presented as chats: a sidebar of who is
here, a transcript in the middle, and everything else about an agent
behind a tab rather than a settings dialog somewhere else.

*** Six sections, not eleven tabs

There are eleven pages and a header bar is not eleven buttons wide.  An
=AdwViewSwitcher= pinned to =ADW_VIEW_SWITCHER_POLICY_WIDE= does not
ellipsise, does not grow an overflow menu and logs nothing when it runs
out of room -- it is simply clipped.  So below roughly 1500 logical
pixels the pages a person could reach depended on the monitor, and on a
wide enough screen there was nothing to see.

The eleven are grouped into six, and a section holding more than one
page draws its own row of tabs underneath -- the same shape the Computer
page already had for its four halves.

| Section | Pages |
|---------+-------|
| Chat | the transcript and composer |
| Agent | Overview, Mailbox |
| Computer | one page, which draws its own Shell / Screen / Mounts / Exchange row |
| Automation | Routines, Triggers |
| Work | Tasks, Decisions, Flow |
| Library | Skills, Memory |

The grouping is ~ClawtSection~ in the library, walked out of
~clawt_section_count()~ and ~clawt_section_page_count()~ rather than
listed in either client -- for the reason the Computer sub-views already
are, and with the same check behind it.  A grouping spelled out twice is
one that drifts, and the drift here would be a page filed under Work in
this window and under Library in a browser, with nothing to say so.

**A section that holds one page draws no second row.**  A switcher over
a stack of one is a control that does nothing, and it would cost Chat
and Computer -- the two pages somebody spends the most time on -- a
strip of the window to say so.

**A section's tab carries the sum of its pages' badges.**  A number one
level down is invisible until somebody opens the section, which for
Decisions would defeat the point of having one: an agent waiting on a
person has to be visible without anybody thinking to look.  The GTK
client keeps each page's number and recomputes the section's in
~clawt_gtk_set_page_badge()~, so a second badged page needs no second
implementation; the web client draws the same count on Work from
~clawt_web_app_open_decisions()~.

That count needed a daemon change to be possible at all.  ~decision.asked~
was published and *nothing* was published when one was answered or
dismissed, so a client's tally could only ever go up: a second window,
the CLI, or the venture bridge answering on the operator's behalf left
every other client drawing a badge for an inbox that was already empty.
~decision.settled~ closes it, and the GTK client -- which already
refreshes on any ~decision.~ event -- became correct the moment it
started arriving.

There is deliberately no icon column in the library's table.  The web
client draws no icons at all, and libclawt must never link GTK, so a
column would be a GNOME string carried by everything that links the
library and read by one caller.  The GTK client picks one from a
~switch~ with no ~default:~ instead, which gives the same guarantee: a
section added to the table does not build until somebody has chosen an
icon for it.

Nothing moved in the URL.  Each page keeps the nickname it had, so
=/a/<id>/tasks= is still =/a/<id>/tasks= and a bookmark still lands
where it did; the section is which tab is drawn as current, not part of
the path.

*** Watching a turn while it runs

An agent's steps -- the tools it reaches for and the prose it writes
between them -- appear at the live end of the chat while its turn runs,
under whatever it has already said.

A run of consecutive tool calls collapses to one line ("Ran 6 commands",
"Ran 13 commands (1 failed)"); its tooltip lists each command.  Prose
appears as it is written, a paragraph at a time.  Reasoning is marked as
such, because it is explicitly not what the agent is telling anybody.
Only a failed tool is coloured -- a colour on every row is a colour that
has stopped meaning anything.

The block clears when the turn settles and the answer takes its place.
Steps are never stored, so what you see is the running turn only;
switching rooms and coming back refetches it from the daemon, and after
the turn ends there is nothing to refetch.

The web client shows the same thing in the same place, refreshed on its
own SSE event so a turn's tool calls do not re-fetch the whole
conversation.

Both are switched off by ~stream_steps: false~.  The reasoning, and why
a step is deliberately not a message, is in
[[file:orchestration.org::#turn-steps][Watching a turn while it runs]].

*** A VM agent's hardware

Cores, memory, disk and screen size sit together in the agent editor,
because they are what the machine is made of.

Screen size is a list of the common ones.  A size set by hand or from
another client that is not among them is added to the list rather than
ignored -- a combo box cannot say "something else", and without that,
opening the page and saving it would quietly replace a chosen size with
the first entry.

It is also the only one of the four that does not need the guest
rebuilt: it reaches the virtual GPU rather than the cloud-init seed, so
it applies at the VM's next boot.

*** Teams in the sidebar

Agents are grouped under their team, with a header showing how many of
that team are running out of how many there are -- running out of total
rather than one number, because "3" could be either and the question
somebody scanning a sidebar is asking is how much of this team is awake.

A header also says whether the team is *working*, not only whether it is
up. Agents are started once and stay running, so running-out-of-total
barely moves; whether anybody is mid-turn is the part that changes minute
to minute, and it was drawn on every agent row and thrown away at the one
level where the rows may be folded out of sight.

The GTK client spins and the web client badges, each matching what its
own agent rows already do --- the two read as one language by matching
their own rows rather than by borrowing each other's widgets. Beside it
is how many, because one busy out of seven is a different fleet state
from seven out of seven and a bare marker collapses the two. At zero it
is left off entirely rather than drawn as "0 working": a count nobody is
looking for, on every idle heading.

The rule itself is ~clawt_team_tally()~ in the library, not a copy in
each client. It had already been written twice, with the out-parameters
in a different order in each and the two asking about a teamless agent
with different spellings --- and neither ~make parity~ nor anything else
could see the difference, since a tally sends no frame and answers no
command. Absent, ~null~ and ~""~ all name the group with no team, and
busy is a subset of running: a stopped agent is not mid-turn whatever a
reply says.

Click a header to fold the team away. The tally stays, which is what
makes folding safe: a collapsed team still says what is behind it ---
including whether it is busy, which is exactly what folding hides.

Which teams are folded is kept in the client, not in ~clawtilla.yaml~.
It is a view preference like the fonts -- it belongs to the person at
this screen rather than to the fleet, and syncing it between machines
would be actively wrong.

Agents with no team come *first*, because that is where the chief of
staff lives; putting them last would bury the agent you talk to most
under every team in the fleet. Teams follow in ~teams.order~, and agents
within a team in their own ~agents.order~, so dragging still works
inside a group.

An agent on a team the daemon does not declare still appears, under that
team's id. Hiding it is how a typo in ~agents.team~ survives being looked
at.

*** Teams

The Teams page in Settings creates them, edits their name and
description, and removes them. Removing one leaves its agents running and
says how many are now teamless, rather than silently moving them
somewhere nobody chose.

An agent's team and its role on it are also in the agent editor, beside
the chief-of-staff switch -- the three answer one question between them:
who this agent may hand work to.  Moving one agent is quicker from the
sidebar's right-click menu; the editor is where the *role* is set, and
where a team is changed as part of a wider edit.

*** Spending

The Spending page lists one row per agent with what it has cost and how
many turns it took, a fleet total, and a period selector -- all time,
today, the last 7 or 30 days.

The cost is the figure the provider reported for each turn, not an
estimate.  The token counts beside it cover new input and output only:
cached context is billed but is not reported as tokens, so the cost is
larger than those numbers suggest.  See [[file:spending.org][Spending]].

*** Reordering the fleet

Drag a row in the sidebar to move it -- a chief-of-staff to the top, the
testers to the bottom.  Dropping on the upper half of a row puts it
above, the lower half below, which is what makes the last position
reachable at all.

The order lives in ~clawtilla.yaml~ as ~agents.order~, not in the
client.  It is about the agents rather than about reaching them, so it
is the same in every client and on every machine -- the opposite of a
connection profile, which exists precisely because the daemon is
somewhere else.  Numbers go up in tens, leaving room to place one agent
between two others by hand.

Agents that share a number keep the order the file has them in, so a
fleet nobody has dragged comes back exactly as it was written.

*** Dragging an agent onto a team

The same drag also *changes* an agent's team, which is the quickest way
to divide a fleet up.

- Drop it on a *team heading* to put it on that team.  The heading names
  a group rather than a position, so where it lands inside the team is
  left alone.
- Drop it on an *agent row belonging to another team* to do both at once:
  it joins that team and goes exactly where you put it, above or below by
  which half of the row you let go over.
- Drop it on a row in its *own* team and nothing changes but the order,
  as before.

Dropping among another team's agents has to change the team, not only the
order.  The daemon returns the fleet grouped, so an agent merely
reordered under somebody else's heading is sorted straight back under its
own the moment the sidebar redraws -- a drop that visibly worked and then
undid itself, which reads as the sidebar being broken rather than as the
gesture meaning something narrower than it looked.

The heading lights up as a drop target while an agent is over it, using
GtkListBox's own highlight so it follows the theme.  Without that the
only way to discover the gesture is to try it and watch the drag snap
back -- which is also what "not a drop target" looks like.

*** Every team has a heading, including the empty ones

A team the fleet declares gets a heading whether or not anybody is on it,
and there is always a *No team* heading.

Both are there because a heading is what an agent is dropped onto.
Without the first, a team created a moment ago in Settings cannot be
filled by dragging -- which is exactly when somebody would reach for it.
Without the second, a fleet where every agent has a team has nowhere to
drag one back out to, and the gesture works in one direction only.

Their tallies read =0/0=, which is honest, and the fleet's shape is
visible at a glance rather than only through the teams somebody happens
to have populated.

The order is the daemon's, not a second opinion about it: teamless first,
then teams as ~team.list~ gives them, which is the array the daemon sorts
by.  A team an agent names that nobody declared still appears, under the
id it named, after all the declared ones.

*** Deleting an agent, and what goes with it

The confirmation offers two things, both off:

- *its container or VM* -- otherwise it keeps running under a name
  derived from an agent that no longer exists
- *everything it owns* -- its persona, notes, mailbox, memories,
  transcripts and credentials

The second is there so a throwaway agent made to test something can be
thrown away.  Removing an agent from the fleet is reversible and
deleting what it wrote is not, which is why it is asked for rather than
assumed.

#+begin_src bash
clawtilla agent rm scratch --with-computer --purge
#+end_src

Every removal is fenced: a path that is not inside the state directory
or the configured workspace root is refused rather than removed.  Those
paths come from a file somebody edits, and there is no undo on the far
side of this.

*** Settings

The hamburger beside the *+* button, at the top of the sidebar.  It holds
what belongs to the application rather than to any one agent:

| Page | What it is for |
|------+----------------|
| Cloud images | VM disk images on this machine, and fetching more |
| Teams | how the fleet is divided, and what each team handles |
| Spending | what each agent has cost, and the fleet total |
| Integrations | Matrix, mail, MCP servers and notifiers, and who gets each |
| Connectors | connected accounts, and the credential for each |
| Appearance | colour scheme and fonts |

**** Cloud images

That page lists the VM disk images on this machine with their size and
where each came from, offers the suggested ones, takes an ~https~ URL of
your own, and draws a bar per download.  A download is the daemon's, not
the window's: closing Settings does not stop one, one begun with
~clawtilla image vm get~ appears here while it runs, and a failure is
said out loud as a toast even if this window is shut -- otherwise an
image would simply never appear, with nothing anywhere saying why.

See [[file:computers.org][computers.org]] for what to do with one.

**** Integrations

Everything configured once and pointed at one agent, some agents or the
whole fleet.  *+* adds one; the type decides which fields appear, so a
Matrix instance asks for a homeserver and rooms while a notifier asks for
a backend and quiet hours.

Two things here do real work rather than editing a file.  *Sign in* runs
a Matrix password login and stores the resulting token in a 0600 file --
the token never comes back to this window, which is the whole point of
doing it in the daemon.  And *Send a test* delivers a notification
immediately, ignoring both the event list and quiet hours, because a
notifier that is working correctly is indistinguishable from a broken one
by looking at it.

Each agent's own Integrations group, in the agent inspector, shows which
of these that agent actually has and whether each is healthy.

See [[file:integrations.org][integrations.org]].

**** Connectors

Connected accounts.  *+* lists the catalogue with the auth kind beside
each name, because that decides what happens next: a device connector
needs a client id, an ~api_key~ one needs a token pasted.

Clicking a connector offers /Authorize again/, /Paste a token/, /Renew
now/, /Revoke/ and /Remove/.  Authorizing shows the user code as the
dialog's heading at full size -- it is about to be read off this screen
and typed into a phone -- and waits there while you do it.

The list shows three states rather than two.  A credential that has
expired *and can renew itself* reads as connected, because it is: the
daemon renews it on a timer.  Showing that as broken would light the list
up every hour and teach you to ignore the one that genuinely is.

See [[file:connectors.org][connectors.org]].

**** Appearance

Colour scheme, and the fonts.

| Setting | What it covers |
|---------+----------------|
| Colour scheme | Follow the system, Light, or Dark |
| Interface font and size | Everything but code -- sidebar, messages, dialogs |
| Code font and size | Code blocks and inline code in a conversation, and the exec console |

Changes apply as you make them and are saved at once. A preview you can
see but which is not yet saved is the worst of both: it looks applied,
and closing the window loses it.

*An empty font, or a size of 0, means "use whatever the desktop says",*
and that is the default. It is deliberately not the same as naming the
font your desktop currently uses -- a named one stops following it, so
changing your desktop font later would leave this one app behind. The
clear button beside each font goes back to following; a font chooser has
no way to express that, which is why the button exists.

**** A palette has to speak both dialects

~@define-color~ and the ~--token~ custom properties are two namespaces,
not two spellings of one, and *libadwaita's own rules read only the
second*.  A palette emitted as ~@define-color~ alone therefore styles
this client's own rules --- which do read ~@token~ --- and leaves every
libadwaita-drawn accent, error, success and warning at stock GNOME.  A
Catppuccin build drew GNOME blue and red icons on a Mocha surface.

Measured on libadwaita 1.9.3 with the shipped values, forced dark: the
palette as it stood was byte-identical to having no palette at all, and
only a ~:root~ block moved anything.

So both forms are emitted for every token, and the second is *derived*
from the first --- thirty-seven hex values maintained twice would drift
the first time somebody added a colour.  The transform is exactly
libadwaita's own naming, ~accent_color~ to ~--accent-color~, and a line
that is not a ~@define-color~ is skipped rather than guessed at.  Which
tokens bridge between the two namespaces is undocumented and has moved
between releases, which is why the only correct answer is both.

**** Three stylesheets, with stated priorities

- the structure sheet at ~PRIORITY_APPLICATION~, loaded once
- the generated appearance sheet at ~PRIORITY_APPLICATION + 1~
- ~$XDG_CONFIG_HOME/clawtilla/style.css~ at ~PRIORITY_USER~, when it
  exists

They were one concatenated string before, which re-parsed a constant on
every settings change and left nowhere for a person to put a rule of
their own: anything they wrote would have had to out-specify code they
did not write, at the same priority.  Precedence between the first two
is unchanged --- appearance came last in the concatenation and so won
ties, and it sits above structure now.

~@define-color~ does cross a provider boundary, which had to be checked
rather than assumed: the structure sheet is written entirely in
~@token~ references while the palette that defines them is in the other
sheet, and an unresolved reference falls back to libadwaita's value
with no parse error and no warning.

The client also names its own widgets --- ~#clawt-transcript~,
~#clawt-composer~, ~.clawt-chat-run~, ~.clawt-avatar~ and the rest ---
because a stylesheet cannot target what has no name, and before that
the only things addressable were libadwaita's element names.  Nothing
is styled by having a name; a selector nobody has written a rule for
draws nothing.

**** What the column actually gives you

600 is the clamp; a row spends 12 on its margin and 44 on the avatar
gutter, so 532px reach the words --- about 82 characters at the default
font.

For a long time the body only *used* 445 of that, about 67 characters,
and the difference showed as a narrow ribbon with a wide empty band
either side on a big window. ~hexpand~ gives a label the column;
~halign~ decides what it does with it, and a wrapping ~GtkLabel~ at
~GTK_ALIGN_START~ is allocated its *natural* width and aligns that
inside the space it was given. ~GTK_ALIGN_FILL~ takes the column.

Measured against real GTK4 rather than reasoned about:

| halign | allocation | layout | characters |
|--------+------------+--------+------------|
| START  | 445px      | 445px  | ~67        |
| FILL   | 532px      | 521px  | ~78        |

Short messages look identical either way --- ~xalign~ is 0, so the text
still starts at the left; only the allocation changed.

The operator's own turns are unaffected. Those are bubbles, and a bubble
needs a width of its own to be right-aligned at all, which is what the
59-character cap on that branch is for.

**** Three breaks, and they have to be in order

A reader sees three kinds of break, and until recently they were in the
wrong order:

| break | GTK | web |
|-------+-----+-----|
| a paragraph inside one message | 18px | 22px |
| a new message inside a run | 24px | 29px |
| a new run | 30px | 36px |

Those are measured, not chosen: a paragraph break is markdown's own
blank line, so it is exactly one line height, and the other two are a
third of a line apart from it and from each other.

The within-run gap used to be *6px* in both clients --- a quarter of a
paragraph. So three consecutive turns from one agent read as a single
message with unusually tight paragraphs, and the grouping said the
opposite of what it meant. The web had the same fault one step up: its
run gap was 26 against a 22px paragraph, four pixels of difference that
nobody could see.

Raising the within-run gap to the run gap would have dissolved the run
--- if two messages from one sender are spaced like two runs, the
grouping carries no information and the header is decoration. The
window was 19–29 in GTK and 23–35 in the web, and the value is the
midpoint of each.

***** Space alone cannot carry it

A third of a line is perceptible and not *nameable*: you can see that
something changed, not what. So a continuation row also carries its own
time, in the gutter the avatar reserves and otherwise leaves empty on
those rows. That is what says "new message" rather than "new
paragraph".

The operator's own turns already carry a boundary: ~clawt-bubble-cont~
drops a corner, so those get the spacing and no timestamp.

***** The times are a rail, and it costs no width

A column of them has to read as one straight edge, which takes two
things: tabular figures, so every ~HH:MM~ has the same advance, and
right alignment on the avatar's edge.  Measured against real GTK 4.22 at
the default interface font, the ink right edges of ~00:00~, ~23:14~,
~11:11~ and ~18:48~:

| placement                           | rail        | body column |
|-------------------------------------+-------------+-------------|
| packed in the gutter, left-aligned  | 34 31 22 31 |          44 |
| packed in the gutter, right-aligned | 34 34 34 34 |      *47*   |
| overlaid on the gutter              | 31 31 31 31 |          44 |

The middle row is the trap.  A ~GtkBox~ is measured from its children
and ~gtk_widget_set_size_request()~ is a floor rather than a cap, so a
time packed into the gutter *sets* the gutter's width --- and an ~HH:MM~
caption is 35px against the 32 the arithmetic assumes.  The rail comes
out straight and every body in the run moves three pixels away from its
own run header, which is the one invariant the gutter exists for.

So the time is an *overlay* child.  Those do not contribute to the
measurement, so it costs no width at all, it is aligned inside a slot
the gutter already reserves, and a time too wide for that slot overhangs
into the row margin instead of moving anything.  The web client reaches
the same place from CSS with ~position: absolute~, for the same stated
reason.

***** One stamp, one format, both clients

~clawt_chat_time_label()~ is the whole answer to what a transcript
stamps a message with, and both clients call it in both places they
draw one --- the run header and the continuation gutter.  They had four
answers between them: GTK rendered the clock and the web rendered a
relative time, so the same conversation carried two conventions
depending on which client it was opened in.

The clock won, for two reasons beyond consistency.  A relative time
rendered on the *server* is wrong the moment it is sent and gets
wronger: nothing re-renders a message that has not changed, so a page
left open goes on saying "2m ago" for an hour.  And a transcript is a
record --- an absolute time is what lets a line here be matched against
the event log, a task, or a journal entry.  Recency belongs to the
lists that are about recent activity, and the mailbox, task and alert
views keep their own relative formatting.

The date is not in it, because a day divider already carries it.
24-hour rather than the locale's format, because the string has to fit
the slot the avatar reserves --- 32px in GTK, 28px in the web --- and a
12-hour locale renders "4:23 PM", which does not.  Measured in a real
browser, the relative form it replaced ("46s ago") wrapped to two lines
in that slot, so a run of continuations drew a column of broken
two-line stamps.

A message with no timestamp gets *no* stamp in either client, rather
than the moment it was drawn.  Both fall back to the current time for
run grouping, which needs a day whatever happens; inheriting that
fallback into the stamp would put a plausible wrong time on a record.

***** One knob, and the ordering survives it

The message gap is *derived* from the run gap rather than being a
second setting --- ~calc(var(--chat-run-gap) - 7px)~ in the web and
~chat_message_spacing()~ in GTK, floored so it can never reach the
paragraph gap. Setting the run gap to 20 with a fixed message gap of 24
would put a new message further apart than a new run, which is the same
inversion arrived at from the other side.

Verified in a browser: 22 / 29 / 36 shipped, and 22 / 53 / 60 with the
run gap set to 60.

**** Reading: the column and the gap between runs

#+begin_src yaml
measure: '90%'     # nine tenths of the width the window has to give
run_spacing: 18    # gap between one person's run and the next; 0 defers
#+end_src

The measure was a constant in C for the whole life of the client, so the
one thing about reading most likely to be wrong for a given person and
screen was the one thing they could not change. Whatever number is
chosen is right for one reader on one display --- which is the argument
~clawt-appearance.h~ already made about fonts, extended without
modification.

***** The measure carries its own unit

Pixels alone is the one unit that is wrong on every screen but the one
it was typed on: a column that fits a laptop leaves two thirds of a 4K
panel empty, and a reader who wants the conversation to use their
display has no way to say so once rather than once per machine.

| Spelling | Means                                        |
|----------+----------------------------------------------|
| ~'90%'~  | nine tenths of the width there is, and grows with the window |
| ~'80ch'~ | eighty characters a line, and grows with the font           |
| ~'640px'~ | exactly 640 pixels                          |
| ~''~     | follow the shipped column, which is ~90%~     |

The unit travels *with* the number rather than sitting in a key of its
own. A separate ~unit:~ would have read an existing file's
~measure: 640~ as 640 of whatever the new default was --- so a column
somebody set once would silently become the whole window, with nothing
anywhere to say so. A bare number is still pixels, which is what a bare
number has always meant here.

An empty measure means defer, exactly as an unset font does, and for the
same reason: a value naming the current default would freeze it, so a
later change to the shipped column would not reach anyone who had ever
opened the dialog.

Out-of-range is clamped rather than refused, *per unit* --- 90 is a
legitimate percentage and far below the pixel floor, so one shared range
would have turned the shipped column into 320px the moment somebody
typed it. This file invites hand-editing, and a 40 or a 4000 in it
would make the conversation unreadable and leave no obvious way back
from inside the app.

A share is never capped from above. A reader who asked for nine tenths
of a very wide display asked for exactly that, and clamping it back to a
typographic maximum would be a control that reports a setting it did not
apply. It is floored, and it never exceeds the window.

***** A column is the digit zero, in both clients

CSS defines ~ch~ as the advance of the ~0~ glyph, and the web client
renders a character count with it. The GTK client therefore measures the
same glyph through Pango rather than reaching for
~pango_font_metrics_get_approximate_char_width()~, which is an
*average*: measured against real GTK4 in Adwaita Sans the two are
7.156px and 9.253px, so "80 characters a line" would have come out 29%
narrower in one client than the other, with nothing anywhere to say so.

A character count also carries the avatar gutter with it, because a
count is about the *words* and the words start past the avatar. In CSS
that is ~calc(80ch + var(--chat-gutter))~; in GTK it is the same
addition in pixels.

***** One measure, two mechanisms

The web client reads ~--chat-measure~ and ~--chat-run-gap~ as CSS
tokens, and the unit survives all the way into the declaration ---
resolving a percentage to pixels while writing the sheet would freeze it
at whatever the window happened to be.

The GTK client cannot use a token: ~AdwClamp~ takes a *property*, not a
stylesheet, so it resolves the measure with ~clawt_measure_resolve_px()~
and sets all three clamps --- the transcript's, the composer's and
Flow's, from one resolver, because a column widened while the box you
type into stayed put would restore exactly the misalignment the gutter
inset exists to fix. (Flow's clamp had a comment claiming it took "the
same measure the chat has" and in fact sat at libadwaita's default, so a
reader who widened the conversation found Flow still at 600.)

That is also why changing it pushes to the live widgets rather than only
rewriting the sheet. A setting that reached the file and the CSS but not
the widget would be a control that appears to do nothing, which is worse
than one that is missing.

***** A share has to be recomputed, and GTK will not tell you

A percentage is only a number once there is something to take it of, so
the column must be recomputed on every re-allocation --- and ~GtkWidget~
offers no signal for that. ~notify::width~ is the obvious hook and is a
trap: measured against real GTK4 across four hand-driven allocations it
fired *zero* times, while the scrolled window's own horizontal
adjustment fired four for four and reported the allocated width exactly.
So the client follows ~notify::page-size~ on the hadjustment, which is
the same "follow the adjustment" answer the vertical case already
needed.

Flow resolves against its own viewport rather than the chat's. They are
pages of one stack and so almost always the same width, but an unshown
page is allocated nothing at all --- so sharing a number would mean
whichever page somebody opened first decided the column for the other.

The gap applies to messages drawn after the change; reopening the
conversation redraws the rest.

**** Palettes are files

A colour scheme used to be a fourth value of an enum, so adding one was
a code change and a rebuild, and adjusting one you mostly liked was not
possible at all. Every other part of the appearance had become
configuration; colour was the exception, and it is the part people most
want to touch.

Drop a stylesheet in ~$XDG_CONFIG_HOME/clawtilla/palettes/~ and it
appears in both clients' scheme lists. The file's name is its identity,
so ~gruvbox-dark.css~ is what lands in your ~appearance.yaml~.

#+begin_src css
/* clawtilla-palette: label="Gruvbox Dark" dark=1 */

/* libadwaita and the GTK client read this form. */
@define-color accent_color #fabd2f;
@define-color window_bg_color #282828;

/* The web client reads this one. */
:root { --canvas:#282828; --ink:#ebdbb2; --surface:#32302f; }
#+end_src

The header is optional: without a label the filename is used, and
without ~dark~ a palette is assumed dark, because almost every
hand-written one is and light chrome under dark colours is worse than
the reverse.

Both vocabularies are in one file on purpose. The colours are shareable
and the mechanism is not --- GTK redefines libadwaita's named colours
and the web sheet has its own tokens --- so translating between them
would mean deciding for the author which half of their palette was real.
Write the half you need; the other is inert.

**** A partial palette is fine; an almost-complete one is the trap

Every libadwaita colour a palette does not name keeps stock GNOME's
value.  For a deliberately small palette --- an accent and a background
over the dark scheme, like the example above --- that is the point.  For
one meant to be complete it is a bug you cannot see coming: the window
is right everywhere except the one widget drawn from the colour you
missed, and nothing anywhere says which colour that was.  The alerts
panel spent its whole life drawn from ~--secondary-sidebar-bg-color~
for exactly this reason.

~clawt_appearance_palette_missing()~ answers it.  The palettes shipped
in this repository are checked against it by ~make test~; a palette on
disk is not, because refusing or warning about the documented two-line
example above would be crying wolf at the supported case.

The colours libadwaita 1.9 reads, all fifty:

#+begin_example
accent_bg_color            accent_color             accent_fg_color
active_toggle_bg_color     active_toggle_fg_color
card_bg_color              card_fg_color            card_shade_color
destructive_bg_color       destructive_color        destructive_fg_color
dialog_bg_color            dialog_fg_color
error_bg_color             error_color              error_fg_color
headerbar_backdrop_color   headerbar_bg_color       headerbar_border_color
headerbar_darker_shade_color                        headerbar_fg_color
headerbar_shade_color
overview_bg_color          overview_fg_color
popover_bg_color           popover_fg_color         popover_shade_color
scrollbar_outline_color    shade_color
secondary_sidebar_backdrop_color                    secondary_sidebar_bg_color
secondary_sidebar_border_color                      secondary_sidebar_fg_color
secondary_sidebar_shade_color
sidebar_backdrop_color     sidebar_bg_color         sidebar_border_color
sidebar_fg_color           sidebar_shade_color
success_bg_color           success_color            success_fg_color
thumbnail_bg_color         thumbnail_fg_color
view_bg_color              view_fg_color
warning_bg_color           warning_color            warning_fg_color
window_bg_color            window_fg_color
#+end_example

Four of those are ours rather than libadwaita's spelling.
~active_toggle_*~ and ~overview_*~ exist in libadwaita only as
~--custom-properties~, with no ~@define-color~ behind them; writing them
in the palette's own dialect lets the automatic bridge produce the form
that is actually read, and keeps a palette one list of colours.

~--banner-color~ is not among them and cannot be.  libadwaita sets it on
the ~banner~ element itself, so no ~:root~ value reaches it --- changing
it needs a rule rather than a colour.

Nothing hermetic can notice a *new* libadwaita release growing a token.
When one lands, re-read the list from the library itself:

#+begin_src bash
gresource extract /usr/lib64/libadwaita-1.so.0 \
    /org/gnome/Adwaita/styles/gtk.css |
    grep -o 'var(--[a-z0-9-]*' | sort -u
#+end_src

The ~@define-color~ half is bridged automatically into libadwaita's
~--token~ custom properties, because libadwaita's own ~.accent~,
~.error~, ~.success~ and ~.warning~ rules read only that namespace. A
palette emitting one form styles the client's own rules and leaves every
libadwaita-drawn status colour at stock GNOME.

A scheme this build has not got follows the system rather than failing
--- a palette file you delete leaves the client following your desktop,
not refusing to start.

The settings live in ~$XDG_CONFIG_HOME/clawtilla/appearance.yaml~:

#+begin_src yaml
theme: 'dark'
font: 'Cantarell'
font_size: 12
monospace_font: 'JetBrains Mono'
monospace_size: 10.5
#+end_src

Not in ~clawtilla.yaml~, for the same reason connection profiles are not
-- and the reason is sharper here, because the client can switch daemons
while it is running. Fonts that came from a daemon's config would change
when you connected to a workstation and change back when you went home.
They are per-machine by nature too: the size that is right on a laptop is
not the size that is right on a 4K panel, and that is often the same
fleet seen from both.

A size outside 6--48pt is clamped rather than refused. This file invites
hand-editing, and a 2 or a 2000 in it would make the settings dialog
unreadable and therefore unable to fix itself.

*** The sidebar

Each agent shows a face, a state dot, what it is doing, and badges:

- a filled *unread count* when that agent has said something to you that
  you have not opened -- see below
- a red *HOST* badge when the agent can run commands on the real machine
- a *CHIEF* badge on the chief of staff
- a green *LEAD* badge on a team lead

Mail waiting for the *agent* is in the subtitle -- =3 waiting to be
read=, or =working… · 3 waiting= when it is mid-turn -- rather than as a
second number beside the unread count.  Two numbers in one narrow row,
both about "messages" and meaning opposite things, is not readable.

Colour is never the only signal: the state is in the tooltip and in the
inspector, and a row with unread messages has its name in *bold* as well
as the pill.

**** CHIEF and LEAD are one question, answered once
:PROPERTIES:
:CUSTOM_ID: standing-badges
:END:

Both badges answer *who may put work on somebody's list*. The chief of
staff may assign to anybody; a team lead may assign within its own team;
everybody else may assign to nobody --- the three rules in
~clawt_team_may_assign()~, and no more.

An agent is never given both. The chief of staff /is/ the lead of every
team, so it is completely described by the stronger of the two, and
drawing the weaker one beside it would read as a second fact rather than
as the same one. Which badge to draw --- including that precedence --- is
~clawt_team_badge_for()~ in the library, read by both clients through a
~switch~ with no ~default:~, so a standing added to ~ClawtTeamRole~ draws
a ~-Wswitch~ warning rather than going quietly unmarked.

That last part is not hypothetical. ~team_role~ has been in every
~agent.list~ reply since the sidebar learned to group by team, and
/neither client drew it/ for as long: the chief was marked and every lead
under it looked like an ordinary member. Nothing reported it, because a
badge nobody draws looks exactly like an agent that does not have the
role.

**** Descriptions, and the setting that moves them to the pointer
:PROPERTIES:
:CUSTOM_ID: sidebar-descriptions
:END:

Under each name is the agent's ~description~ --- the line that tells you
which agent to ask. Both clients draw it now; the web sidebar drew
nothing at all until this was added, which is a gap ~make parity~ could
not see, because a subtitle sends no frame, answers no slash command and
spells out no library value. It is a declared affordance for exactly
that reason.

A fleet of ten agents with a paragraph each is also why the list stops
fitting on a screen. So *Appearance → Agent list → Descriptions* turns
them off --- and off does not mean gone: the description moves to the
row's tooltip in the GTK client and its ~title~ in the web one. The
complaint this answers is the height, not the text.

It is on by default, and the default is what a /zeroed/ appearance
means, deliberately: a preference file written before this key existed
keeps its descriptions rather than silently emptying every row on
upgrade. It is the one field in ~ClawtAppearance~ whose zero does not
mean "defer to the system" --- a boolean has no third state to spend on
deferring, so the choice is which of the two costs nothing, and it has to
be the behaviour the clients already had.

Like the fonts and the folded teams, it lives in the /client's/ config
(~agent_descriptions~ in the appearance file; a ~clawt_agent_desc~ cookie
in the browser) and not in ~clawtilla.yaml~. It is about the person at
this screen, not about the fleet.

What it does /not/ hide is what an agent is doing. =working…= and =3
waiting to be read= stand in place of the description while they apply,
and a list that stopped saying which agents are working would answer a
different question from the one the setting was turned off to answer.

*** Alerts

A collapsible panel on the right of the GTK client, bound to a bell in
the header bar; an =/alerts= page in the web client, reached from the
sidebar.  Both carry a count.

**** An alert that lands in front of you arrives read

An unread count means "one you have not seen".  Both clients used to
record every alert unread and clear the lot when the surface was
*opened*, which is right for one that arrives while the panel is closed
and wrong for one that lands in front of somebody: it was inserted
unread, bumped the badge, and went on counting until the reader closed
the panel and opened it again --- a number pointing at a row they had
watched appear.

~clawt_alert_arrives_read()~ decides it for both clients.  The routine
tier arrives read whatever is on screen, because neither badge counts
it and a flag no badge reads is one a later widening of the filter
would silently start believing.

One visible consequence, worth knowing rather than discovering: a row's
unread highlight follows the same flag, so an alert arriving into an
open panel no longer draws it.  That is consistent --- the highlight
means /not yet seen/, and it has been --- but keeping both the honest
badge and an arrival highlight would need a third state, which is a
larger decision than the fix.

**** Why a surface, and not a toast

Of the 89 places the GTK client raises a toast, **two** are
notifications: a download that failed, and a message the loop guard
refused.  Everything else --- /Saved./, /An agent needs an id./, /That is
not a port./ --- answers a question the operator is holding right now, in
a dialog, waiting to know why the button did nothing.

So the split is by *origin*, not by severity:

| where it came from | where it goes |
|--------------------+---------------|
| it arrived on its own | the panel, and no toast |
| it answers something you just did | it stays a toast |

Filing the second kind into a drawer would be a serious regression, and a
panel whose first twenty rows are /Saved./ buries the one line that
matters.  ~clawt_window_toast()~ is unchanged and so are its other 87
call sites.

**** One list, with a filter

An alert is an event that demanded attention; a routine entry is one that
did not.  Same stream, different weight --- so one list and a filter that
widens it, rather than two tabs.  Two tabs would mean deciding at write
time which tab a thing belongs in, and the interesting case is exactly
the routine event that turns out to matter: a filter lets you widen after
the fact, a tab makes you guess before.

Three tiers, and **only two are coloured**:

| tier | how it is drawn |
|------+-----------------|
| Error | a card with a red edge, body-size text |
| Notice | the same shape, accent instead of red |
| Routine | no card --- one line of quiet caption text |

If everything carries a colour, colour stops meaning anything, and
routine entries are the majority the moment the filter widens.  Making
them quiet is what keeps the loud ones loud.  Severity is carried by
weight and container as much as by hue, so it survives a colourblind
reader and a Catppuccin palette alike.

Which tier an event lands in is ~clawt_alert_tier_for_event()~, in
libclawt, for the same reason: both clients classify the same stream, and
two implementations of that rule differ exactly once --- on the kind
somebody adds next.  It takes the *event* rather than a kind and a loose
string, because which detail decides the tier varies per kind
(~agent.state~ reads =state=, ~image.finished= reads =error=) and a
caller passing the wrong one would classify silently and wrongly.  Only
the wording of a row is decided in the client.

**The count never includes routine entries.**  Otherwise it would be
permanently non-zero and would stop being read, and widening the filter
is a thing you choose rather than something that should change the
number.

**** Scope, and how far back

Fleet-wide.  Alerts fire about agents other than the one being read ---
that is the whole reason the surface exists --- so scoping to the
selected agent by default would hide exactly the cross-agent case that is
hardest to diagnose.  Every row names its agent, and clicking that name
narrows to it and raises a chip that clears the scope again.  One control
for scope, no second dropdown.

The most recent 200 are held in memory, session-scoped.  For anything
older there is ~event.list~, which reads the daemon's own
~ClawtEventLog~ --- NDJSON under the state directory, written since the
daemon was first built, swept on ~daemon.event_log_days~, and until now
read back by nobody.  That is the honest answer to "does it survive a
restart": the surface does not and does not need to, because the log
does.

**Opening it marks everything read.**  Rows stay until they are
dismissed, or until *Clear all*, so nothing is lost by the count going to
zero.

**** Where it sits

In the GTK client the panel is a second ~AdwOverlaySplitView~ *inside*
the existing one's content, not wrapping it: opening alerts must not hide
the agent list, because that is navigation, and losing your place while
reading a notice about something else is the same failure as a toast
covering the composer.

320px, which is load-bearing rather than conventional: 281 for the agent
list plus 600 for the transcript's clamp plus 320 comes to 1201, so on an
ordinary window all three fit and *the transcript does not move when the
panel opens*.  Below 1200px it overlays instead --- the number is derived
from those three widths, not chosen.

It carries libadwaita's ~.isolated~ class, and that is the whole reason
the two edges of the window are the same colour.  libadwaita styles panes
by where they sit in the widget tree: a ~.sidebar-pane~ inside a
~.content-pane~ is a *nested* sidebar and is painted from
~--secondary-sidebar-bg-color~, a deliberately different shade.  Nesting
here is a layout decision --- it is what keeps the agent list on screen
--- and not a statement of hierarchy, but libadwaita cannot tell those
apart.  Measured before the class was added: the agent sidebar ~#181825~
and the alerts panel ~#28282c~ under Catppuccin, and ~#2e2e32~ against
~#28282c~ on stock GNOME dark, so the mismatch was there under every
scheme rather than only under a palette.

*** Unread

A filled pill at the trailing edge of the row, and the fleet-wide total
on the *Chat* tab.  It counts messages an agent sent *to you* while you
were looking somewhere else -- a different agent's conversation, or a
different page.

This is not the same as the transcript's *New messages* rule, and the
two never fire for the same message:

| where the message landed | what says so |
|--------------------------+--------------|
| the conversation on screen, below the fold | the *New messages* rule and pill, no count |
| any other conversation | the sidebar count, and the total on Chat |

So a conversation that is on screen never accrues a count, whatever the
scroll position.

*Opening the conversation is the only thing that clears it.*  Not
scrolling, not the window gaining focus, not time passing: a counter that
decays on its own is a counter you stop trusting.

*The Chat tab is not badged while you are reading Chat* with the sidebar
open, because the sidebar beside it is saying exactly /which/ agent --
strictly more information.  With the sidebar collapsed to a drawer the
badge stays, since then nothing else is saying so.

**Both clients ask the same function.**  ~clawt_unread_should_count()~ is
in libclawt and takes the four conditions --- not your own, not the room
on screen, not a replay, and a room that is somebody's conversation with
the operator.  It was written twice before that, once per client, which
is the shape of drift =make parity= exists to catch and cannot see: a
rule that decides a number sends no frame and answers no command.

Two limits, stated rather than buried:

- *It is what arrived while this client was connected.*  It does not
  survive a restart, and it deliberately ignores the events the daemon
  replays when a client subscribes -- those may well have been read in
  the previous session, and counting them would open a fresh window
  already showing a number for a conversation nobody has touched.
- *In the web client the count belongs to the server, not the browser.*
  ~clawtilla-web~ binds the loopback and the tailnet for one operator; a
  second browser would share the counts rather than get its own.

The number is not capped at =99+=.  The usual justification is width and
it is measurably false: at the client's caption size =99+= is wider than
the three-digit number it would replace.

Agents with unread messages do *not* float to the top.  The list is
grouped by team and hand-ordered, and a list that rearranges itself under
your hand costs the spatial memory that makes a sidebar fast to save you
a glance.

*Right-click an agent* for Start, Stop, Restart, its *Team*, and Delete.
These are on the Agent tab as well, but reaching them there means leaving
the conversation, acting and coming back -- three navigations for the
thing you do to an agent most often.  A long press does the same on
touch, where there is no second button.

The *Team* submenu lists every team the fleet declares, with =No team=
first, and *ticks the one this agent is already on* -- so the menu
answers "where is this" as well as offering to move it.  Choosing an
entry writes ~agents.team~ and the sidebar regroups; the agent keeps
running throughout, since a team decides who may hand it work rather than
anything about how it runs.

An agent naming a team nobody declared gets an entry of its own, under
the id it named.  Without that the tick would land on =No team= and
moving the agent away would look like it had done nothing -- and a typo
in ~agents.team~ is exactly what somebody opens this menu to fix.

The teams offered are the ones the sidebar was drawn from a moment ago,
not a fresh question to the daemon: a menu has to appear at once.

Right-clicking selects the agent first, so the menu always acts on the
row under the pointer rather than on whatever was selected before.
Entries are greyed out by the same rules the inspector's buttons use:
Stop is unavailable on a stopped agent, Start on a running one, and a
shadow agent -- one whose configuration did not parse -- offers no
lifecycle verb at all, because the daemon would refuse every one of
them.  Delete sits in its own section so it is never the neighbour of
Restart, and still asks twice.

*** The tabs

| Tab | What it is |
|-----+------------|
| Chat | the transcript, with bottom-follow that does not yank you down mid-read, and an activity line under it |
| Agent | edit name, description, provider, model, effort, computer, restart policy, autostart and chief-of-staff; credentials by reference; start/stop/restart; delete |
| Mailbox | what is waiting, with acknowledge |
| Computer | four halves: Shell, Screen, Mounts and Exchange |
| Routines | standing work for this agent: the schedule, when it last ran and what happened |
| Tasks | what has been delegated to whom, and where it got to |
| Flow | every conversation between agents, newest first, and what was said |

The Routines tab puts the presets in front -- Manual, Hourly, Daily,
Weekdays, Weekly -- and cron behind /Custom/, because most standing work
is one of the five and nobody should have to write ~0 9 * * 1-5~ to get
weekday mornings.  A routine that did not fire because the machine was
asleep is shown as *missed*, not failed: showing it as broken would train
you to ignore the one that is.  See [[file:routines.org][routines.org]].

*** The Computer tab has four halves

Shell, Screen, Mounts, Exchange -- in that order, in both clients, walked
out of ~clawt_computer_view_count()~ rather than listed in either.  A
list spelled out twice is one that drifts, and here the drift would be a
tab that exists in a browser and not in this window with nothing to say
so.  ~make parity~ compares which enumerations each client walks.

| Sub-view | What it is |
|----------+------------|
| Shell | the exec console, showing stdout *and* stderr |
| Screen | what is on the agent's desktop, and the controls for taking it |
| Mounts | the paths from this machine that appear inside the computer, host spelling first |
| Exchange | the shared drop-box every computer gets |

Shell is first because it is the one every computer has -- a container
with no desktop still runs commands -- and it is where a link with no
sub-view on it lands.  Screen is drawn only where there could be one,
which the daemon answers per agent from
~clawt_computer_type_has_screen()~.

The web client puts the sub-view in the *path* (=/a/<id>/computer/screen=)
rather than in a cookie, so a link to somebody's screen survives being
pasted into a chat.

**** The Screen sub-view

The picture is polled while somebody is looking at it and at no other
time: a frame comes out of the same connection the agent is working
over, so watching costs the thing you are watching.  The GTK client
subscribes when the tab is selected and lets go when it is not; the
browser subscribes on each poll and its lease lapses when the tab is
closed, because a closed tab cannot say goodbye.  Both are the same
mechanism -- see [[file:computers.org][computers.org]].

A stale frame is *labelled with its age*, never presented as current.
The threshold is the library's, so a browser and a window agree about
when a picture stopped being news, and a frame that has never been taken
is not stale -- there is nothing to label, and "0 seconds ago" over an
empty panel describes a picture that does not exist.

Both clients decode at display size.  A size request in GTK is a floor
rather than a cap and ~GtkPicture~ takes its natural size from its
paintable, so a full-resolution decode into a preview panel costs the
memory of the whole framebuffer -- an avatar pays that once per agent,
and a frame arrives once a second.

*Taking the screen* stops the agent acting on it; giving it back also
settles any request the agent made for hands.  Input is offered only
while you are holding it, rather than shown greyed out: a control that
is there and does nothing teaches somebody to click it twice.

The coordinate fields say which pixels they are in.  The picture is
downscaled where the pixels are, so the two sizes differ, and both
clients are told both -- a control that silently mis-clicked by a factor
of three would read as the pointer being unreliable rather than as
arithmetic.

*Open in a viewer* appears for a VM whose domain is running.  The GTK
client spawns ~remote-viewer~ and names the package when it is missing;
the browser offers the same ~vnc://~ address and a ~.vv~ file.  No noVNC
is vendored -- the page has to work on a tailnet with no route out.

*** The transcript

A conversation is drawn as *runs* rather than as one row per message.  A
run is consecutive messages from one sender: it gets one header --- an
avatar, the name, the time --- and the messages tuck under it at the same
indent, tight inside the run and further apart between them.  That
grouping is what makes a stack of paragraphs read as a conversation.

A run ends at a different sender or at a day boundary, and deliberately
*not* at a time gap: a fifteen-minute rule would be a constant with
nothing behind it, and agent traffic arrives in bursts where it would
fire constantly.  ~clawt_chat_run_is_start()~ decides it, in libclawt, so
the two clients cannot disagree about where a run begins.

**** Bubbles on one side only

The operator's own turns are bubbles; the agent's are not.

An agent's turn runs to dozens of lines with headings, lists and code
blocks.  A container that long stops reading as a message and starts
reading as a panel, and a bubble wide enough to *be* a bubble is too wide
to have a measure --- the two constraints pull opposite ways, and only
one side of this conversation is short enough to satisfy both.  So the
bubble goes where it works.  The asymmetry is not a compromise; it is
what tells you who is speaking at a glance.

Within a run the second and later bubbles drop the corner nearest the one
above, which makes a run read as one utterance rather than a stack.

**** Avatars

Initials and a colour derived from the name, which needs no
configuration at all.  ~agents.color~ paints the background when it is
set, and in the GTK client ~agents.avatar~ replaces it with a real image.
Both keys had been in the schema since the beginning and were read by
nothing; this is what finally reads them.

A colour goes through ~clawt_color_ink()~, which accepts only ~#rgb~ and
~#rrggbb~ and answers which of black or white is legible on it.  It is
also the validation: nothing else checks that key, and it is spliced into
a stylesheet.

**** The measure

Both clients cap the column, at nine tenths of the width the window has
to give unless the reader says otherwise --- see [[*Reading: the column and the gap between runs][Reading]] for the units.

It used to be a fixed column: ~AdwClamp~ at its own defaults in the GTK
client, 40rem in the web one, which is a good column and a bad default.
Those numbers were *measured* rather than chosen --- at 52rem a line ran
to 117 characters against a comfortable range of 45 to 90, and 40rem
renders 89 at the same font, taken from the browser's own text metrics
--- and they are still what a reader picking ~ch~ is choosing between.
What made them the wrong default is that they leave most of a wide
display empty, and saying otherwise meant converting a monitor's width
to rem.

The composer follows the same column.  A full-width entry under a narrow
column of text reads as a rendering fault rather than as a layout.

A percentage resolves against each element's own containing block, so
the web sheet reserves the scrollbar gutter on *both* the transcript and
the composer --- the composer is made a scroll container purely so it
has a gutter to reserve. Without that a classic scrollbar took 15px off
the transcript's column while the composer kept all of it: measured in a
real browser at 1280px, 431.09px against 444.59, a 13.5px step between
the words and the box you type them into, appearing the moment a
conversation grew past one screen. Reserving on both sides makes them
equal, and reserving it *always* stops the column stepping sideways the
first time a conversation runs past the fold. Measured at 0.00px
difference in width and in left edge, scrolling and not.

On a narrow screen the avatar gutter collapses and the face is hidden:
measured at 375px, the indent would otherwise take the measure to 42
characters, and the avatar is the first thing that should go rather than
the text.

**** Following the live edge

The transcript scrolls itself only while the reader is *at* the end.
Yanking somebody down mid-read because a message arrived is the single
most annoying thing a chat window can do, so both the queued scroll and
the growth handler refuse to move the view when they are not.

"At the end" is ~clawt_transcript_is_at_bottom()~, with a 32px tolerance
--- not zero, because a scrolled window rarely lands on an exact value
and a reader one pixel off has not chosen to be; not large, because every
pixel of it is a pixel of message a new arrival can push off the bottom
without the client noticing it stopped following.  It is a pure function
so the tolerance is exercised on both sides *and at its boundary*, which
is the one part of the follow machinery a test can reach without a
window.

The mechanism that actually does the work is ~notify::upper~ on the
scrolled window's adjustment, not the queued idle beside it: GTK4 lays
out from the frame clock, so an idle reads the height the transcript had
*before* the message was allocated.  The idle stays as a belt to the
growth handler's braces, and a consequence worth knowing follows from
it --- **a window the compositor is not presenting does not lay out, so
it does not scroll.**  Measured on both backends: the frame counter stops
and ~upper~ stops with it while messages are appended perfectly well to
the widget tree.  It resolves on the next frame; there is nothing for the
client to do about it, because there is nobody looking.

All of that lives in ~ClawtGtkFollow~ (=clients/gtk/gtk-follow.c=), one
per transcript, because the Flow tab needs exactly the same behaviour
and had none of it.  A view that draws something about *not* following
--- the chat's pill and rule --- hands the follower an ~armed~ hook,
which runs on the way back to the edge; the Flow tab passes NULL,
because a peer conversation is not where a message is waiting for an
answer.

**** A refresh appends; it does not redraw
:PROPERTIES:
:CUSTOM_ID: flow-refresh-appends
:END:

The Flow tab's transcript used to be emptied and rebuilt from all two
hundred messages every time the fleet said anything, because
~refresh_flow_once()~ re-selects the row that is open and that emits
~::row-selected~ again.  A box that has just been refilled is a box at
offset zero, so any message anywhere in the fleet threw the reader back
to the oldest message of whatever they were reading --- and the
conversation opened there in the first place.  Nothing logged it and
nothing looked broken; it read as a page that would not stay put.

So opening a room draws it and refreshing one appends to it, keyed on
the message id ~room.history~ already sends.  The widgets the reader is
looking at are then the same widgets afterwards, which means there is no
scroll offset to save and restore: what is left is the ordinary rule
above --- stay at the bottom if that is where you were, and do not move
at all if it is not.

Two details that are bugs if they are missed.  Which room is open is
read *after* the history call returns, not before it, because
~clawt_window_request()~ iterates the main context and a nested refresh
may have opened another room while this one was waiting --- comparing
against what was open before would append one conversation into
another's transcript.  And a message with no id is drawn only on the
pass that draws everything, since appending it on a refresh would append
it again on the next one.

**** Day dividers

"Today", "Yesterday", or "Wednesday 25 August", between two rules,
inserted when the date changes and once at the top of a loaded history.
A date change is a bigger break than a speaker change, so it gets more
room than the gap it sits among.

**** One row builder, and the Flow tab uses it

The GTK client had two: the chat's and the Flow tab's.  They had drifted
into visibly different renderings of the same content --- one with runs,
avatars, dividers and a measure, the other a flat list of captions ---
so a reader moving between a conversation and the flow of one saw two
conventions for one kind of thing.

Fixed by deleting one of them rather than by teaching the second the same
tricks, which is the only version that cannot drift again.  What differs
between the two views is a small struct: which box, whose run state, and
how the other party is drawn.  The Flow tab derives an avatar from each
sender's name, because a room there has several participants rather than
one configured agent.

That also gave the chat two things the Flow tab had and it did not: a
*task chip* and a *hop count*.  A delegated reply arriving in your own
chat is exactly the one worth knowing was delegated, and a hop count
climbing towards ~max_hops~ is the only thing on screen that
distinguishes a loop from a conversation.  The web client already drew
both in its chat, so this closed an asymmetry between the clients that
=make parity= could not see.

The web client's Flow view is a *digest* across every room --- one
truncated line per message, so the whole of what the fleet has been
saying fits on a page --- rather than a transcript of one room, which is
why it does not draw through the chat's builder.  What it does share is
where a run begins: consecutive messages from one agent do not repeat the
name, from the same ~clawt_chat_run_is_start()~.

*** Markdown

Agent replies are rendered: bold, italic, inline code, fenced code,
headings, bullet and numbered lists, block quotes, rules and GFM
tables. Enough for what a chat actually contains, and nothing beyond
it.

Both clients render, from one grammar.  ~clawt_markdown_to_pango()~
and ~clawt_markdown_to_html()~ are the same walk over the same document
with a ~RenderOps~ per output: the walk decides *what* is happening and
the ops decide how it is written.

That indirection is the whole reason it exists.  A renderer each is how
the chat transcript and the Flow tab came to draw one message two ways,
and how tables came to exist in one client and not the other --- and
the failure is invisible, because a construct that renders in one place
and vanishes in the other looks perfectly normal from either.  Adding a
construct now means adding a callback, and a vocabulary that has not
implemented it does not build: a short positional initialiser is a
~-Wmissing-field-initializers~ warning, and this project treats a
warning as a build failure.  Verified by removing one and watching the
compiler refuse it, rather than assumed.

~tests/test-markdown.c~ carries the runtime half --- for a corpus of
constructs, every distinctive word in the source has to reach *both*
outputs, so a callback wired to nothing fails the same way a missing
one does.

The important word is *rendered*, not *interpreted*.
~clawt_markdown_to_pango()~ walks the document cmark parsed and emits
Pango markup only for the structure cmark identified, escaping every
piece of literal text on the way out. An agent that writes
~<span foreground="red">~ gets those characters on screen; a message
crafted to close a tag cannot reach the parser at all, because the
parser never sees its text. ~tests/test-markdown.c~ throws the obvious
attempts at it and also asserts that the tags in the output are always
balanced -- Pango refuses unbalanced markup, and a GtkLabel handed
something it cannot parse renders *nothing*, so a message would silently
vanish. The client checks with ~pango_parse_markup()~ before using it
and falls back to plain text, which is belt as well as braces.

Links show their target beside the text and are not clickable. A
clickable link in model output is one keystroke between a prompt
injection and a browser; a person who can see where it goes is a person
who can decide.

*** Tables

A table is the one construct cmark does not parse.  Tables are GitHub's
extension rather than CommonMark, and they reach C only through
cmark-gfm --- which Fedora does not package, and which exports the same
=cmark_*= symbols as the cmark libreclaw already links, so having both
in one process is a collision the dynamic linker resolves silently and
in whichever direction it likes.

So the source is split before it is parsed.  A table block is found in
the markdown, drawn by ~clawt-markdown.c~, and everything either side of
it goes to cmark exactly as it always did.  Each cell is then a cmark
parse of its own, which is what keeps the rule above: a cell's text
still reaches the output through the one function that escapes
literals.

A table that fits the column is drawn as a grid:

#+begin_example
Team   Lead
─────  ────────
forge  oxpecker
qa     oryx
#+end_example

In the code font, and that is not decoration --- a proportional font
gives every glyph its own advance, so the padding would line nothing
up.  Padding is measured in the columns a cell *draws* rather than in
its length: markup weighs nothing on screen, a combining mark adds
nothing, and a CJK glyph or an emoji takes two.  Each of those is a
different grid and two of them are crooked.

A table too wide for the column becomes one =Header: value= line per
cell instead, records separated by a rule:

#+begin_example
Team: forge
Agents: oxpecker reviews every merge request and refuses a warning
────────
Team: qa
Agents: oryx holds the reference result on Fedora
#+end_example

That is the case a chat actually produces --- an agent summarising a
fleet writes a sentence per cell --- and a grid that wraps is worse than
no grid, because the wrap lands in the middle of a row and every column
after it is somewhere else.  The threshold is 58 columns, measured
rather than chosen: the shipped column is AdwClamp's 600px, a body
starts one row margin plus one gutter in and stops a margin short of
the far edge, and a monospace glyph at the default font advances 9px.
A person who widened the measure gets records where a grid would have
fitted, which is the safe direction.

Two tables are deliberately left as they were.  One indented, because a
table written under a list item is indented too and treating it as top
level would cut the list in half around it; and one inside a block
quote, which nothing here takes apart.  Both render as they did before
tables were drawn at all, so neither is a regression.

Both clients draw it, from one walk over one document ---
~clawt_markdown_to_html()~ is the same traversal emitting the other
vocabulary.  The web client has neither threshold nor record layout: a
browser scrolls a table sideways inside the message, so it stays a
table at any width.


*** The composer stands on the message column

The transcript and the composer sit in the same clamp, centred alike,
and they still did not line up --- in *either* client --- because only
one of them spends anything on the avatar.  A row is inset from the
clamp and an agent's body is then indented past a gutter, so a body
starts a gutter and a margin in; the composer had neither, so the
entry's frame stood on the avatar's left edge rather than the text's.
That put the strongest vertical line on the page inside the one column
deliberately kept empty.

Both trailing edges were already right.  Only the leading edge was out,
and by exactly the gutter.

The two clients need different numbers --- GTK insets 12 and gutters
44, the web sheet gutters 36 with no inset --- so what is shared is the
derivation and not the values.  ~clawt_chat_body_inset()~ states it
once; the GTK client builds ~CHAT_BODY_INSET~ from its pair and the web
sheet declares ~--chat-gutter~, which its body, its attachments and its
composer all read.

It aligns the *frame*, not the text inside it: a bordered control reads
as a box and the box's edge is the line the eye follows, so aligning
the text would push the frame back into the gutter and restore the
fault.

Narrow enough that the avatar is hidden, there is no gutter to stand
past and the composer returns to the clamp with the bodies.  In the web
sheet that override has to sit *after* the rule it overrides ---
identical specificity means source order is the only thing deciding,
and grouped with the sheet's other narrow overrides it would lose every
time while reading exactly like a fix.  ~/web/narrow-composer-override-can-win~
asserts the ordering rather than trusting it.

*** Writing the message

The box is a text view, not an entry: Ctrl+G hands whatever comes back
from ~$EDITOR~ straight into it, and that is usually several paragraphs.
Enter sends, Shift+Enter is a newline, and the box grows to about ten
lines before it starts scrolling.

*Ctrl+G* writes the current draft to a temporary ~.md~ file, opens
~$CLAWT_EDITOR~ / ~$VISUAL~ / ~$EDITOR~ on it, and takes back whatever
was saved when the editor exits. It is seeded with what is already
typed, so this extends a draft rather than replacing it. ~/compose~ does
the same thing from the keyboard.

*** Slash commands

Typing ~/~ reveals the matching commands above the entry, with what each
does; clicking one fills it in. Anybody who already knows the command
keeps typing and never looks.

| Command | What it does |
|---------+--------------|
| ~/help~ | list them |
| ~/start~ ~/stop~ ~/restart~ | this agent's lifecycle |
| ~/attach~ | pick files to send with the next message |
| ~/compose~ | write the message in $EDITOR |
| ~/edit [file]~ | open a workspace file in $EDITOR |
| ~/files~ | this agent's workspace files |
| ~/memory <query>~ | search what this agent has remembered |
| ~/agents~ | who is in the fleet |
| ~/flow~ ~/tasks~ | go to those pages |
| ~/clear~ | clear the transcript on screen only |
| ~/new~ | create an agent |

Every one is a shortcut to something that already exists -- none of them
is a second way to do anything. A command never reaches the agent, and
an unrecognised one says so rather than being sent: a mistyped command
arriving as a message is how somebody learns not to trust the feature.

Nineteen in total, counting the five under *More commands* below, and
the web composer answers the same nineteen by the same names.  Three
mean something slightly different in a browser -- ~/compose~ opens a
full-page box rather than ~$EDITOR~, ~/copy~ shows the text to select
rather than reaching a clipboard on a machine that may not be yours, and
~/edit~ opens the file in the page for the same reason.  ~make parity~
fails if the GTK composer grows one the web composer has not.

*** Sending files and images

Paste an image and it is queued as a PNG. Ctrl+V is intercepted only
when the clipboard actually holds an image -- ordinary text paste is
left to the widget, and an image copied from a browser usually carries a
URL alongside it, so taking both would be worse than taking either.

The paperclip, or ~/attach~, picks files. Queued files appear as chips
above the entry and can be dropped before sending; nothing is written
anywhere until the message is actually sent, so an abandoned draft
leaves nothing behind for the agent to find.

On send, each one goes to the daemon over ~attachment.put~, which writes
it into that agent's exchange directory. The message body then names
them, with *both* paths:

#+begin_example
have a look at this failure

[clawtilla] Files sent with this message. Open them with your own read
tool at the host path below; it runs on the host even when your shell
does not.
- pasted-1750000000.png
  /home/you/.local/share/clawtilla/exchange/researcher/pasted-1750000000.png
  (inside your container: /mnt/clawtilla/exchange/researcher/pasted-1750000000.png)
#+end_example

Both, and the host one first, because they are for different tools. An
agent's own read tool runs on the host even when its shell runs in a
container -- so given only the container path it can stat the file with
~clawtilla_computer_exec~ and never open it, which is exactly what
happened to the first image anybody sent. The container path is there
for shell commands.

Naming them at all is what makes them reachable: an agent reads files
with its own tools, and one it has not been told about is one it will
not open.

*** Seeing them again

A message that carried an image shows a thumbnail under it, and clicking
it opens the picture full size in its own window -- Escape closes it. A
path in a message body is a thing nobody remembers the contents of ten
minutes later, and the whole point of a transcript is that you can
scroll back.

The client recognises its own attachment block and reads the *host*
paths out of it, so this works for anything still on disk and quietly
does nothing for anything that is not -- including for a client running
on a different machine from the daemon. Thumbnails are decoded at
thumbnail size rather than shrunk in the widget: GTK has no
maximum-size property, a size request is a minimum, and a GtkPicture
takes its natural size from the paintable, so a full screenshot handed
to it stays a full screenshot however the widget is configured. They
also turn ~can-shrink~ off, which defaults *on* and sets the minimum
width to zero -- with it left alone the preview was squeezed down to a
few dozen pixels by the layout around it.

The name is rebuilt from its basename rather than trusted --
"../../.ssh/authorized_keys" is a filename -- and 32 MB is the cap,
because this crosses the IPC socket base64-encoded.



*** Reading what an agent said to another agent
:PROPERTIES:
:CUSTOM_ID: peer-conversations
:END:

An agent has one conversation with you and one with each peer it has
talked to.  Work handed down a chain is answered back up it --- a chief
of staff delegates to a lead, the lead to a member, and each answer goes
to whoever asked --- so a delegated answer is in a *peer* conversation,
not in yours.

Above the transcript there is a switcher: /Chat/, which is yours, and
one /with <agent>/ entry per peer.  In the GTK client it is a menu
button; in the web client a row of tabs, and the peer conversation has
its own URL (~/a/<id>/chat?with=<peer>~) so it can be linked and
bookmarked.

It appears only when there is something to switch to.  An agent that has
never delegated has one conversation, and a control offering a single
choice costs a line of the transcript and answers a question nobody
asked.

A peer conversation is **read-only**.  What the composer sends is a
message to the agent whose page you are on, which would land in your own
chat while you were looking at a different one --- so it is disabled
there and says why.  To say something to either agent, open its own
chat.

Which rooms count is ~clawt_chat_conversation_peer()~ in the library
rather than a rule in each client: it is a question about a room's
membership, not about its id, and a client that took ~dm:a:b~ apart
would break the day the daemon spelled a direct room differently.

*** Right-click

| Where | What you get |
|-------+--------------|
| an agent in the sidebar | start, stop, restart; its machine's own start, stop and restart; its team; delete |
| a message | copy it as markdown, text or org |
| an attachment | open, save a copy, copy path, delete permanently |
| the conversation | copy, open in $EDITOR, or save -- each as markdown, text or org |

Attachments open with the desktop's own handler, so a PDF opens in
whatever reads PDFs here and an image in whatever shows images. A file
that is not an image gets a chip with its icon, name and size instead of
a thumbnail; clicking it opens it the same way.

Deleting asks first, goes through the daemon rather than unlinking from
the client -- a client that deletes by path is a client that can be
asked to delete any path -- and says plainly that the message will still
name the file afterwards.

**** The Computer submenu

An agent whose computer is a container, distrobox or VM gets a
=Computer (container)= entry with /Start/, /Stop/ and /Restart/ under
it.  These are the *machine*, not the agent: an agent and the container
it runs commands in are different things, and the submenu is what keeps
the two sets of verbs from being a guess every time.

Absent, rather than greyed out, for an agent with no machine --- a
chat-only or host agent has nothing to power on, and three permanently
dead entries would be three things to read past on every right-click.

/Stop/ asks first when stopping the machine destroys it, which is a
container's default: see [[file:computers.org::#computer-power][computers.org]].  A VM is never
destroyed by a stop and is not asked about.

The web client has the same three as a /Power/ card on the agent's
Computer page --- there is no right-click there, and a page is the
surface a browser has.

*** Taking a conversation with you

~Ctrl+Shift+G~ opens the whole conversation in ~$EDITOR~ as org.
~Ctrl+G~ is the message you are writing; the difference is how much of
it you meant.

Conversion is pandoc's, run with ~--wrap=none~ so *one paragraph stays
one line*. pandoc rewraps at 72 columns by default, which turns every
paragraph into a block of hard-wrapped lines: unreadable in an editor
that soft wraps and a nuisance to edit or diff afterwards.

Markdown needs nothing installed. Text and org need pandoc, and say so
rather than quietly handing back markdown under an org label -- which
would be the worst of the three possible answers.

The document is read back from the daemon rather than scraped off the
widgets, so an export is of the conversation rather than of the last
screenful of it.

*** More commands

| Command | What it does |
|---------+--------------|
| ~/interrupt~ | stop what it is doing now; leave it running |
| ~/reset~ | start the agent's AI session again, from nothing |
| ~/retry~ | send your last message again |
| ~/export [org]~ | save the conversation |
| ~/copy [org]~ | copy the conversation |

~/interrupt~ is the composer's *Stop* button written out, and both go to
the same place. The button is beside Send in each client and appears only
while there is a turn to stop --- a control that is always there and
usually does nothing is one people stop reading, and "is something
running I can end?" is exactly the question its presence answers.

It is not ~/stop~. That takes the agent down along with its container or
VM and needs a start afterwards; this kills the AI CLI running the turn
and everything it started, and leaves the agent connected with its
session and mailbox intact. ~docs/agents.org~ has the table.

Both clients say how many processes were signalled rather than "stopped",
because zero and several mean different things to whoever pressed it: the
agent was between turns, or that much was running. Neither offers the
button for an agent whose runtime declares no ~interrupt~ --- an embedded
agent takes its turn inside the daemon, where the only process to signal
is the daemon itself.

Both also hide the button the moment the request succeeds rather than
waiting for the next typing frame. The turn was just taken out from under
the code that lowers the indicator, so that frame may never arrive, and a
Stop button still sitting there after a successful stop reads as the
press having failed.

~/reset~ is for an agent that has got itself wedged. It stops the agent,
moves its session files aside, clears its stored sessions from
libreclaw's database, and starts it again if it was running -- so the
next turn opens a new CLI session instead of resuming the old one with
~--resume~.

Both places matter: libreclaw restores a session from the files *or* the
database, so clearing only one leaves the agent resuming from the other
and looking like the reset did nothing. The files are moved rather than
deleted, because a reset is what you reach for when something is wedged,
which is exactly when you might want to look at what it was doing.

*** Where a toast lands

~AdwToastOverlay~ places its toast at the bottom of whatever it wraps,
and one overlay wrapped the whole window --- so on the chat page every
confirmation landed on the text being typed.

There are two overlays now, because one cannot mean two places: one
wraps the chat page's transcript region, so a toast settles above the
composer, and one wraps the view stack for every other page, where the
bottom of the page is the right place and there is nothing there to
cover.  ~clawt_window_toast()~ picks between them on the visible page,
so all 87 call sites are untouched --- the choice belongs to where a
toast lands, not to the places that raise one.

Chat is singled out not because it matters more but because it is the
only page holding something a toast can destroy the use of: everything
else a toast covers can be scrolled back to, while the composer holds
text that has not been sent anywhere yet.

*** Following the live edge

~notify::upper~ and ~notify::page-size~ are emitted by ~GtkViewport~
from inside its own size-allocate, after it has positioned its child
for that pass.  Writing the adjustment's value there moves the number
and not the offset, and nothing queues a further pass --- so the view
lands one message short: the adjustment reports the bottom, the newest
reply stays clipped against the composer, and it stays that way rather
than correcting a frame later.

That state is invisible to every other correction in the file, because
they all test the adjustment and the adjustment is already right.  The
handler added to correct the idle was what stopped the idle working.
It queues a scroll instead of writing the value, so the write happens
outside the layout pass.

Whether the reader is at the live edge at all is
~clawt_transcript_is_at_bottom()~, in libclawt and tested on both sides
of ~CLAWT_TRANSCRIPT_FOLLOW_TOLERANCE~ and at its boundary --- every
pixel of that tolerance is a pixel of message an arrival can push off
the bottom without the client noticing it stopped following.

*** Smaller things

Messages carry the time they were sent. What you were typing is kept per
agent, so clicking another agent and coming back does not lose it.


*** The Flow tab

The Chat tab is your conversation with one agent.  Flow is the rest of
it: what the fleet said to each other while you were not looking.

The left-hand list is one row per conversation that has anything in it
-- who, how long ago, who spoke last and the first line of it, with the
message count on the right.  A fleet accumulates a direct room per pair
of agents and most of them are empty, so empty ones are left out. Sorted
by most recent activity, and refreshed on every message, so the top of
the list is what is happening now.

Your own chats are excluded by default.  This page exists to answer
"what did they do without me", and mixing them in buries it; the
checkbox above the list puts them back.

Picking one shows the whole exchange on the right, each message with who
said it and when.  A message that belongs to a delegated task carries a
~task~ chip -- that is the difference between "beta said something" and
"beta said something because alpha asked it to", and it is the thing
that makes this a flow rather than a list of remarks.  The chip opens
the task board, where the rest of the story is.

The conversation you are reading stays open across a refresh, at the
message you were reading.  Messages arrive continuously, and a list that
reselected its first row every time anything anywhere spoke would take
you off what you were reading.

It opens at the *newest* message rather than the oldest, and a refresh
appends what is new instead of redrawing the room, so an arrival
anywhere in the fleet does not move the view at all unless you were
already at the bottom --- the same rule the chat transcript follows, and
now the same code.  See [[#flow-refresh-appends][A refresh appends; it does not redraw]].

Every control's sensitivity is bound to ~ClawtAgentCaps~, and a disabled
control says why in its tooltip.  A greyed-out button with no
explanation is the interface equivalent of a shrug.

Editing writes through ~agent.set~, which is the same path
~clawtilla agent set~ uses -- the client has no second way to change a
config.  The model and the computer only take effect on the agent's next
start, and the Save toast says so rather than leaving you wondering why
nothing changed.

*Deleting asks twice.*  "Are you sure?", then "ARE YOU REALLY SURE!?".
Removing an agent stops it and takes it out of every room, and one click
between browsing and gone is too few.  Its mailbox and transcripts stay
on disk: removing an agent is reversible, deleting its history is not.

The second dialog carries a checkbox for the agent's container or VM,
named -- "Also delete its container, clawt-researcher".  Off by default:
a container may hold work that was never anywhere else.  Agents with no
computer do not get one.

Message bodies are rendered as labels, never as Pango markup.  Model
output arrives from somewhere the user does not control, and treating it
as markup would let a message rewrite the interface around it.

*** The activity line

Under the transcript, a spinner and a line of text, hidden when there is
nothing to say.  An agent turn runs for minutes, and a chat window that
shows nothing between the question and the answer is indistinguishable
from a broken one.

It shows:

- =delivered -- waiting for a reply= the moment a message is accepted by
  a *running* agent
- =thinking= while the agent holds a typing indicator up, which libreclaw
  raises for the whole turn
- nothing at all once the reply lands, or when the target was not running

That last case is deliberate.  Sending to a stopped agent raises a toast
-- "bravo is stopped -- held in its mailbox until it starts" -- and
leaves the activity line clear, because a spinner for something that is
never going to answer is a worse lie than no spinner.  The daemon
reports the target's state in the ~msg.send~ reply, so this is never
inferred from a sidebar that may be a refresh behind.

Tool calls are not shown yet.  libreclaw runs a turn through
~ai_cli_client_chat_sync()~, which spawns the CLI without streaming and
parses one buffer at the end, so no per-tool event exists to display.
Showing them means moving the session turn onto
~ai_cli_client_stream_run_async()~ upstream first; the frame kind,
event kind and the widget are the easy part.

*** Adding an agent

The ~+~ button is a split button.  Pressing it creates an agent, which is
what it has always done; the arrow beside it also offers *Import an
agent...*.

The create dialog offers both paths: fill in id, model and computer by
hand, or answer six questions and let a model draft it.

The questions ask one thing each -- what it should do, what it should
never do, what it needs to work on, how it should come across, what it is
working on, anything else.  Only the first is required, and a blank
answer becomes a heading marked /Fill this in./ rather than an invention.

/Designed by/ picks which model does the drafting, separately from the
model the agent will run on: there is no reason for them to be the same,
and a person will often want their best model to draft an agent that then
runs on a cheap one.

The model writes the agent's org files as well as its configuration, so a
designed agent arrives with a real ~SOUL.org~ rather than a scaffold full
of /Fill this in./.

Choosing ~container~ reveals an *Image* dropdown, listing your own images
from ~defaults.container_images~ first and clawtilla's suggestions after,
with the full reference shown underneath the selection -- a label like
"Fedora 44" does not say which registry it came from, and that is exactly
what differs between two machines.  /Other.../ reveals a free-text field:
the list is a suggestion, and any reference podman can pull is valid.

The same dropdown is on the Agent tab for a container agent, so the image
can be changed later.  *The AI path shows
the proposed configuration and waits for you to confirm* -- a model's
proposal becoming an agent without anyone reading it is the kind of
convenience nobody asks for twice.

*** Importing an agent

Two different things wear that name, and the dialog shows both, because
which one you want depends on where the agent already is.

*Already on disk* lists workspaces clawtilla can see that no agent
claims -- an agent removed from the config keeps its state, a design that
was never committed leaves a whole workspace, and a config restored from
a backup leaves everything it did not mention.  Those accumulate silently
and were previously reachable only through ~clawtilla agent discover~, so
from the client they were invisible.  Importing one adds the config entry
it is missing; nothing is copied or moved.  Each row says what the
directory holds, because "there is a directory called researcher" is not
enough to decide with: one with a mailbox and a memory database is an
agent somebody ran, and one with nothing but a ~config.yaml~ is a design
abandoned halfway.

*From somewhere else* brings in a workspace that is not already where
clawtilla keeps them.  Its provider and model come with it, so an import
does not quietly move the agent onto the fleet defaults.

/How/ chooses between three, and the choice is about what happens to the
original:

- *Copy it in* --- the default, and a *fork*.  The original stops being
  the thing the agent reads, so editing either afterwards is editing one
  of two diverging directories.  /Keep git history/ applies only here,
  and is off by default: the new workspace is usually not the same
  repository.
- *Link to it* --- clawtilla writes into that directory itself.  One
  copy, which is what you want for a workspace you already maintain
  somewhere.  Removing the agent with its files removes *the link* and
  leaves the directory alone, and says so.
- *Clone a git repository* --- the /Git URL/ row takes the URL.  Where
  the workspace root is inside a git repository the workspace is added
  as a submodule of it; where it is not, it is an ordinary clone.

The row that does not apply is insensitive rather than hidden --- a row
that vanishes takes your place on the page with it, and the dialog is
short.

Whatever happened is reported in the toast, in the daemon's own words
rather than the client's.  Whether a git import became a submodule
depends on the machine, so the side that found out is the side that
says; "Imported." would be true of both outcomes and useful for
neither.

*** Shared folders

Settings → Shared folders holds the directories every agent's computer
gets --- the tree you would otherwise wire into each agent by hand and
then forget on the next one you make.

Leave the path inside empty and it is the same path in both places.
That is usually what you want: an agent told about =~/source= finds it
at a name that reads the same on either side, so a note about a file is
a note either of you can follow.

Leave /Teams or agents/ empty and the folder goes to every agent.  Put a
name in it and only that team or agent gets it --- a team covers whoever
is on it, including people who join later.  One field, not two: a name
is a team or an agent, and the fleet already knows which, so being asked
to classify it is being asked to know something the daemon does.

Container, distrobox and VM agents get them.  A host agent does not, and
the dialog says why: there a mount is the confinement allowlist rather
than a shared folder, and widening that is not something a default
should do quietly.

*** Another machine's fleet

Left of the app title is a menu naming the daemon the window is showing.
It lists *Local* and every saved connection, and switching is one click.

The name is also shown as the window's subtitle and the button is
tinted whenever the connection is remote.  That is deliberate: every
destructive verb in the client -- remove an agent, tear down its VM,
reset its session -- acts on whichever daemon is connected, and someone
who has forgotten they are pointed at another machine will not find out
until afterwards.

Every entry says whether that machine is *up*, and opening the menu is
what asks: each saved connection is probed on a worker thread, and the
verdict is drawn beside its name along with the daemon's version and how
many agents it has.  On a worker thread rather than the main one because
a host that is asleep fails only when its route times out --- answering
that on the loop would freeze the window for the length of every
unreachable host in the list.

~clawt_connection_probe()~ runs on a =GMainContext= of its own, which is
what makes calling it from a worker thread safe.  A #ClawtClient settles
on whatever context is thread-default at the moment it connects, and a
fresh worker thread has none --- so the probe used to settle on the
*global* default, the context the window's own loop is running.  It then
called ~g_main_context_iteration()~ on it from the worker: with the main
thread holding that context the push failed with a pair of GLib
criticals, and with the main thread idle the acquire *succeeded* and the
worker dispatched GTK's own sources.  The second case is the dangerous
one and it is silent.

There are four verdicts and the distinction between two of them is the
point:

| ~reachable~ | it answered, with its version and agent count |
| ~refused~ | it answered and would not have us: the token is wrong |
| ~unreachable~ | nothing answered: it is off, asleep, or the route is gone |
| ~not checked~ | nobody has asked yet |

A rotated token and a sleeping host produce the same silence from a
client that does not tell them apart, and sending somebody to check the
network when the answer is a credential --- or the reverse --- costs far
more than the probe does.  A *refused socket* is the network, not a
credential, however much the two are spelled "refused" in English; only
the daemon's own refusal of the bearer token is ~refused~.

~not checked~ is not a failure.  Drawing an unasked connection as
unreachable would be the client asserting something it has not
established.

Before this the only way to find out was to switch to that connection and
fail, which is a destructive way to ask a read-only question: switching
tears down and rebuilds the whole window, so "is that machine up?" could
not be asked without committing to the answer.

*Manage connections...* adds and removes them.  A connection needs a host
and the token from ~clawtilla daemon token~ on that machine; the port
defaults to 8792.  TLS is only worth turning on if that daemon has a
certificate -- inside a tailnet, WireGuard is already encrypting the
connection.

Switching connects the new daemon *before* letting go of the old one, so
a typo in a port leaves you where you were with a toast explaining why,
rather than looking at nothing.  Everything on screen is then cleared:
agent ids, room ids and message ids are all per-daemon, so keeping any of
them would show one daemon's transcript under another daemon's agent.

~clawtilla-gtk~ also takes ~--profile~, ~--host~, ~--port~ and ~--token~,
so a laptop with no fleet of its own can start straight on a workstation
rather than opening on a local daemon it was never asking about.

Those flags are a convenience, not a requirement.  The window opens
whether or not any daemon answers, so the connection menu is always
reachable -- see [[#no-local-daemon][If there is no daemon on this
machine]] below.

*** When the route to another machine goes away
:PROPERTIES:
:CUSTOM_ID: route-goes-away
:END:

A laptop suspends, a tailnet comes back on a different path, a firewall
forgets an idle flow.  None of those close a connection or reset it;
they stop carrying packets, and both ends go on believing in it.  What
that is and why it matters is in
[[file:ipc-protocol.org][ipc-protocol.org]]; what to expect is here.

Every network connection is armed with keepalive at both ends, so one
that has died is given up after about a minute.  Then the machinery that
already existed runs: the client reconnects, retrying after a second and
doubling to a minute, and re-subscribes from the cursor it had reached,
so the daemon replays what was missed.

On screen a banner appears under the header saying the connection is
gone and that what is shown is from before it went, and it disappears
when the daemon comes back.  Both graphical clients draw it in the same
place -- inside the content column, so the agent list does not move:
that list is navigation, and losing a connection is not a reason to
shift it.  The CLI does not reconnect at all.

It is a banner rather than a toast because it is a *state*, not a
moment.  A toast answers a question somebody is holding right now and
then goes; this is a condition the window is in until something changes,
and the whole reason it exists is that the condition used to be
invisible.  A pause of a minute or two in a quiet conversation was
indistinguishable from a quiet conversation.

The replay is bounded.  The daemon keeps the last 1024 events, so an
outage busier than that is not fully replayed -- and now the client is
*told*: the daemon answers ~resumed: false~, the client emits
~ClawtClient::resync~, and both clients answer it by re-reading rather
than waiting for events that are not coming.  The GTK client reloads the
fleet, the inspector and the transcript and says so in a toast (a toast
this time: by the time it appears the view is correct again, and a
standing notice about a hole that has just been filled is a notice about
nothing).  The web client pushes every open page to re-render and drops
its unread counts, which were counted from a stream with a hole in it.

*** Which version the daemon is

Also at connect, once: the client sends ~control.status~ and compares
the version it reports with its own.  A mismatch is drawn in the same
banner, saying which way round it is and what to do:

#+begin_example
This daemon is clawtilla 0.2.0 and this client is 0.1.0. The daemon is
newer, so it may offer things this client cannot ask for; update the
client.
#+end_example

Which way round is the whole point.  A newer daemon may answer a frame
this client cannot send; an older one may refuse a frame this client
will send -- so "version mismatch" would leave somebody guessing which
of their two machines to update.

~control.status~ has reported the version since the frame existed and
had exactly one caller in the tree: the CLI.  Neither graphical client
ever sent it, so a client talking to a different build found out by a
frame kind being refused, in whichever feature the person happened to
open, with a message about that feature.

A version the client cannot read -- a daemon that predates the field, or
a string that is not three numbers -- says *nothing*.  Announcing "this
daemon's version is unreadable" on every connect would be crying wolf at
the case the check exists to keep quiet.

Reconnecting wins over a mismatch when both apply: while the connection
is down the version is whatever it was before it went, and telling
somebody to update a daemon they cannot reach is advice about the wrong
problem.

Worth knowing what this replaced.  Before the keepalive, a connection
that died was never noticed at all: the window read "connected" for the
rest of the day, every event in that window was lost, and the next
message typed into it waited out 120 seconds and never arrived.  The
reconnect and the cursor resume both worked throughout and were simply
never reached, because the disconnection they start from never happened.

*** Adaptive

Below 800px the sidebar becomes a drawer.  Without that the agent list
eats a phone-sized window and the conversation has nowhere to go.

Below ~CLAWT_ALERTS_PUSH_BREAKPOINT~ the alerts panel overlays the
transcript rather than pushing it aside.  That number is *derived* rather
than chosen, by ~clawt_alerts_push_min_width()~, from four numbers other
lines of code own: the agent list's width, the panel's share of what is
left, the transcript's clamp and a row's margin.

Fitting the clamp is deliberately not the criterion.  That is met a long
way below the threshold and looks wrong when it is met, the column
finishing a few pixels from the panel's edge.  The criterion is
~CLAWT_ALERTS_PANEL_GAP~ of clear space measured to the *ink*, which the
row margin contributes to --- so the floor is 1124 and the client breaks
at a round number above it.

The reason it is a function rather than a constant is that every input
belongs to something else.  Widen the panel and the floor moves; nothing
warns, because nothing is broken --- the panel still opens, the
transcript still renders, and the column simply sits against the panel
with nowhere to breathe.  ~/client-rules/alerts-push/shipped-clears-it~
is what fails instead.

Opening the panel does shift the column left, by 122px at a 1280 window.
That is accepted rather than overlooked: the clamp fixes the measure, so
pushing *translates* the column without reflowing it, and neither the
line breaks nor the reader's place in the conversation move.

**** A closed sidebar stays closed

~AdwOverlaySplitView~ opens its own sidebar when it stops being
collapsed, so crossing a breakpoint upward drove ~show-sidebar~ back to
TRUE and an operator's explicit close did not survive a resize.

Both splits therefore remember what they were meant to be showing and
put it back on ~notify::collapsed~ --- that signal rather than
~notify::show-sidebar~, because libadwaita emits the collapsed notify
first, so the correction is already made before anything downstream
reads the property.

That ordering is the whole point rather than a detail.  ~show-sidebar~
on the alerts split is what marks every alert read, and libadwaita's
write satisfied that guard exactly --- so widening a window past the
breakpoint silently zeroed the unread count for alerts nobody had
looked at.  A panel appearing by itself is cosmetic; an unread count
zeroing itself is not.

The client's own write is deliberately kept out of the record: closing
the drawer when an agent is picked on a narrow window is an affordance
getting out of the way, not the operator saying they want no agent
list, so widening the window brings it back.

*** If there is no daemon on this machine
:PROPERTIES:
:CUSTOM_ID: no-local-daemon
:END:

The window opens anyway, with the connection menu in it.

This is the one thing about the client worth knowing before you need it:
a local daemon is the connection *least* likely to matter to a client
whose whole point is reaching daemons elsewhere, and it must not be able
to stand between you and the others.

What you get is a banner across the top of the content saying which
daemon could not be reached, with a *Connections* button that opens the
menu.  Behind it the client keeps trying, so starting ~clawtillad~ a
moment later fills the window in by itself -- the agent list, the
transcript and the daemon's version all arrive without your having to
close the application and open it again.

The banner words itself for where the daemon is, because the remedies
are different:

- *Local* -- "Not connected to Local. Start clawtillad, or choose another
  connection."  Both routes named, because either may be the one you
  wanted.
- *Remote* -- "Not connected to workstation (100.72.0.41:8792). Still
  trying; choose another connection to switch."  No mention of starting
  a daemon: starting one *here* gets you no closer to a machine over
  there.

It never says the connection was *lost*.  That is a claim about history
and it is wrong for a window that has never had anything on it -- and it
would hide the only advice that helps.  A connection that genuinely goes
away mid-session gets its own sentence; see [[#route-goes-away][When the
route to another machine goes away]].

While disconnected the window does not toast.  A window makes a dozen
requests before it has drawn anything, and a dozen copies of "not
connected" would bury the one control that leads out.  The banner says it
once and keeps saying it, which is what a persistent condition deserves;
a toast is for answering something you just did.

** The web client

#+begin_src bash
clawtilla-web [--port 8790] [--bind ADDR] [--no-tailscale] [--socket PATH]
              [--profile NAME | --host HOST [--daemon-port N] [--token T]]
#+end_src

Everything ~clawtilla-gtk~ does, served over HTTP: the same pages in the
same six sections, the same slash commands, and the same settings pages
behind them, driven by the same ~ClawtClient~ over the same socket.  See
[[file:web-client.org][web-client.org]] for the whole of it.

The list of pages is not written out here, and was wrong when it was --
it said seven while there were nine, and there was nothing to notice.
It lives in ~ClawtSection~ and ~ClawtPage~, which both clients walk; see
[[*Six sections, not eleven tabs][Six sections, not eleven tabs]].

It is a separate binary rather than a mode of the daemon, deliberately.
The page can start, stop, reconfigure and delete every agent on the
machine and run commands inside their computers, so it exists only when
somebody has decided they want it.

*** Where it listens

By default the loopback *and*, when there is one, this machine's tailnet
address.  A tailnet is the one network where listening past the loopback
is defensible without a login of our own: every peer is a device the user
enrolled and WireGuard authenticated, and nothing off the tailnet can
route to a 100.64/10 address at all.  That is the same reasoning
~clawtillad~ already uses for its own convenience listener.

A machine with no tailnet gets the loopback alone.  It does not fall back
to every interface -- widening the audience because an address was
missing is the opposite of what anybody would want.

| Flag              | What it does                                       |
|-------------------+----------------------------------------------------|
| ~-b/--bind ADDR~  | replaces the defaults; repeatable                  |
| ~--no-tailscale~  | this machine only                                  |
| ~-p/--port PORT~  | default 8790                                       |
| ~-s/--socket PATH~ | which daemon to talk to                           |
| ~-V/--version~, ~--license~ | print and exit                           |

An address named with ~--bind~ is never optional: failing to bind it is
an error, because a client that ignored where it was told to listen and
started anyway is running somewhere nobody knows about.  The tailnet
address, which clawtilla chose rather than the person, is a warning.

*** It has no login

Anything that can reach a listed address drives the whole fleet.  On a
tailnet that is bounded by WireGuard; anywhere else, put it behind
something that can require one.  It says so on startup, and names every
address it actually bound rather than the ones it was asked for -- a
convenience address whose bind failed is exactly the interesting case.

*** Escaping

Every element is built with htmx-glib's typed classes rather than by
appending to a string, which is not only that library's house style: it
is what escapes agent output.  Names, descriptions and message bodies are
written by people and models, and this client serves them back over HTTP,
so an unescaped ~<~ is script injection into whoever opened the page.
=tests/test-web-render.c= asserts it on the rendered markup rather than
on the code that produces it.

*** Without JavaScript

Every action is a real form or link, so the whole client works with
scripting off -- each action posts and the page re-renders.  htmx is what
makes it feel live: partial swaps instead of full reloads, and an SSE
stream so the fleet updates itself.  Both scripts are served from
=data/web=, never a CDN.  See that directory's README for why.

*** Parity with the GTK client

They answer for the same daemon, so they reach the same parts of it.
That is checked rather than remembered:

#+begin_src bash
make parity
#+end_src

It compares five layers: the IPC frame kinds each client sends, the
slash commands each answers, the library choice enumerations each walks,
the library vocabulary values either spells out, and a list of declared
interface affordances.  Each layer exists because the one before it
reported OK through a real gap -- the frame-kind check missed thirteen
missing slash commands, since they are built out of frames both clients
already sent, and neither of those saw a colour scheme that shipped in
the shared library and was selectable in one client.  The full account
is in [[file:web-client.org][web-client.org]].

Only the fifth needs anything declared, and it says so: an affordance is
checked because somebody wrote a row for it, so the check cannot see a
capability nobody wrote down.

Deliberate exceptions live in an exception map inside
=tools/clawt-client-parity.sh=, each with its reason; anything without a
reason somebody would agree with belongs in the client instead.  It also
runs as part of ~make docs-check~ and as a case in ~make test~.

Routing is checked separately, against a running client, because unit
tests cannot see it:

#+begin_src bash
make web-smoke WEB_SMOKE_URL=http://127.0.0.1:8790 WEB_SMOKE_AGENT=chief
#+end_src

** Embedding libclawt

The library is the product; the daemon is one caller.

#+begin_src c
#include <clawtilla.h>

ClawtDaemon *daemon;
GMainContext *context = g_main_context_default();
g_autoptr(GError) error = NULL;

daemon = clawt_daemon_new(config_path, context);

/*
 * Before start(), not after: "started" is emitted from inside
 * clawt_daemon_start(), so a handler connected afterwards never runs.
 */
g_signal_connect(daemon, "started", G_CALLBACK(on_started), self);

if (!clawt_daemon_start(daemon, &error)) {
    g_warning("clawtilla: %s", error->message);
    return;
}
#+end_src

No process, no socket.  ~clawt_daemon_start()~ returns once the fleet is
up; the host's own main loop drives everything from there.

~config_path~ is a real path, and nothing expands a leading tilde for
you -- that is the shell's job, and there is no shell here.  Build it,
or pass ~NULL~ and let clawtilla resolve its own default:

#+begin_src c
g_autofree gchar *config_path =
    g_build_filename(g_get_home_dir(), ".clawtilla", "config.yaml", NULL);
#+end_src

*** Driving it

Two ways.  Component accessors, for anything structural:

#+begin_src c
ClawtAgentManager *agents = clawt_daemon_get_agents(daemon);
GPtrArray *list = clawt_agent_manager_list(agents);

ClawtEventBus *bus = clawt_daemon_get_event_bus(daemon);
g_signal_connect(bus, "event", G_CALLBACK(on_event), self);
#+end_src

Or the same frames a socket client sends, which keeps a host on exactly
the surface every other client uses:

#+begin_src c
g_autoptr(JsonNode) request = clawt_ipc_request_new("agent.list", "1");
g_autoptr(JsonNode) reply = clawt_daemon_handle_request(daemon, request);
#+end_src

*** The cmacs case

cmacs already owns a main loop and already has libreclaw embedded.  It
calls ~clawt_daemon_new(path, NULL)~ -- ~NULL~ meaning the default
context, which is the one Emacs runs -- then ~clawt_daemon_start()~, and
connects the bus's ~event~ signal.  Agents become buffers; the sidebar
becomes a list.  There is no second process, no socket, and no protocol
in between.

Everything is introspectable (~Clawt-1.0.typelib~), so the elisp side can
work through GObject Introspection rather than through C.

The things to know when embedding:

- signal handlers are the host's business.  clawtilla installs none;
  ~clawtillad~ installs its own.
- prefer ~runtime.type: embedded~ for agents when there is no
  ~libreclaw~ binary to run, and read the constraints in
  [[file:agents.org][agents.org]] first.
- anything of yours that schedules work must attach to the *same*
  context you handed to ~clawt_daemon_new()~, and the obvious GLib calls
  do not.

*** Timers and idles attach to the wrong context by default

~g_timeout_add_seconds()~ and ~g_idle_add()~ attach to the *global
default* main context, not the thread-default one.  For a host running
its own loop on its own context that means the source never fires at
all -- and nothing warns, because adding it succeeded.  Three real bugs
came from this inside clawtilla: two timers that never fired for an
embedded daemon, and an idle that never delivered a single event.

So there is ~clawt_timeout_add_seconds()~, which attaches to the
thread-default context, and every periodic timer in clawtilla goes
through it.  It is public, and it is the one to use from a host too:

#+begin_src c
GSource *tick = clawt_timeout_add_seconds(30, on_tick, self);

/* g_source_destroy() to cancel; the source is transfer-full. */
#+end_src

There is no ~clawt_idle_add()~; for a one-shot, build the source and
attach it yourself:

#+begin_src c
GSource *idle = g_idle_source_new();

g_source_set_callback(idle, on_idle, self, NULL);
g_source_attach(idle, context);
g_source_unref(idle);
#+end_src

The same trap has a second shape one API along.  ~g_task_new()~ captures
whatever context is thread-default *on that thread*, and GLib does not
push a source's own context on the way into its dispatch -- so a task
created inside a callback completes on a loop nobody runs.  Push the
context explicitly around the ~g_task_new()~.  The general rule:
anything that captures "the current context" is a bug in an embedded
daemon unless it was told which one.
