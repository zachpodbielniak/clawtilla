#+title: Review regression guarantees
#+description: Behavioral guarantees covered by the September 2026 bug regressions.
#+authors: Clawtilla contributors
#+categories: development testing
#+created: 2026-09-09T00:00:00-04:00
#+updated: 2026-09-09T00:00:00-04:00
#+version: 1

* Attachment names

An attachment id is a single filename inside the attachment store. Adjacent dots inside that filename are valid, so files such as =report..txt= remain downloadable and retain their display names. Previously stored ids use the same rule. Leading dots, path separators, empty ids and parent-directory traversal remain rejected.

=tests/test-attachment.c= stores files with adjacent and leading dots in their original names, resolves the generated ids, reads back their bytes and checks the displayed names. The hostile-id tests continue to enforce the store boundary.

* Repeat detection

Tool-call keys normalize whitespace outside quoted values while preserving every byte inside JSON strings and single- or double-quoted command arguments. Escaped quotes and escaped whitespace do not accidentally end the literal value. Reads of filenames that differ only in literal spaces therefore have separate repeat counters, while pretty-printed argument formatting still normalizes.

=tests/test-turn-hygiene.c= checks literal spaces, tabs, escaped quotes, shell quoting, escaped spaces, and the actual repeat threshold across distinct reads.

* Build flag changes

Both debug and release builds track compiler and linker flags in their own =.build-flags= stamp. Changing flags, including enabling or disabling sanitizers, invalidates the corresponding objects without requiring a manual clean. An unchanged invocation retains the existing objects.

=tests/test-build-flags.sh= compiles a disposable C program with the actual ~config.mk~ and ~rules.mk~, changes a preprocessor value without touching its source, and checks the new executable in both modes. It also verifies that unchanged flags do not recompile the object.

* Rejected routine schedules

Routine updates validate the proposed schedule before modifying the live mapping. Invalid times, custom expressions and weekdays leave the existing schedule, accompanying instruction or enabled changes, and YAML comments untouched. Validation combines submitted fields with the existing routine; explicitly clearing an override uses its schema default.

=tests/test-daemon.c= sends invalid updates directly through the routine handler and compares the complete serialized configuration before and after each refusal.

* Usage baselines

The daemon establishes each loaded agent's usage watermark before timers, listeners or autostart can deliver work. Starting or connecting an agent also primes its watermark, covering newly added and reset agents. Priming is idempotent: a reconnect cannot discard costs that are waiting for the next reply to charge them.

A missing database establishes an empty baseline without creating a database file. When the agent later creates the file, its first recorded turn is chargeable. Existing history is excluded using row ids as well as timestamps, so old and new turns recorded in the same second remain distinguishable. Read failures remain errors instead of successful empty baselines.

=tests/test-usage.c= covers empty databases, historical baselines and repeated priming. =tests/test-daemon.c= drives reply events for new and existing agents, including reconnects, and verifies both the recorded task spend and the budget refusal after the first paid turn.
